# Improvement loop — progress

Newest first. One line per iteration. Full protocol: `.claude/commands/improve.md` · backlog: `tools/backlog.json`.

- 2026-08-16 · iter 76 · [content] **The lesson that teaches the ride pattern now notates a ride** [BL-099 chunk 1/3] — `jazz-ride-pattern#0` is titled *"Quarters on the Ride"*, its tip says *"just quarter notes on the ride cymbal"*, and every notehead on its stave was `g/5/x2` — **a hi-hat**. All four of that lesson's exercises were, and all five of `independence-chapin-method`, whose body states its premise as *"lock the ride and hi-hat foot into their jazz roles"* and whose first exercise is titled *"The Foundation: Ride + Hi-Hat Foot Alone"*. The hi-hat **foot** is separately notated as `d/4/x2` in those same exercises, so there was no reading on which the hand voice was a hi-hat: a reader following the notation played the wrong drum, heard the wrong sample, and learned the wrong staff position — three wrongs from one key. 47 source lines, **58 noteheads**, nine exercises, two lessons. **The builder extended my scope by one and it turned out to be required.** I briefed eight; it changed nine, adding `jazz-ride-pattern#2` — which my sweep missed because its *own* tip says "hi-hat foot" rather than "ride" — on the grounds that the lesson body prescribes the build order literally (*"Quarters first. Add the skip-note. Add the hi-hat foot on 2 and 4. Add the bass drum on 1."*), so #2 is #1 plus a foot with the hand untouched. The adversarial pass made that stronger than "reasonable": excluding it would have left **one of four exercises on the hi-hat line inside the lesson called The Jazz Ride Pattern**. Third time a builder here has overruled me and been right. **No prose needed repairing, which is the unusual shape of this bug** — every "hi-hat" in both lessons is the foot and every "ride" was already true of the intent, so the notation was the only liar; the opposite of the BL-086 campaign, which once shipped a fixed stave beside prose describing the old one. Three tips were **false before this change and are true now**: `chapin#1` "the snare doubles a note already in the ride", `chapin#2` "three things at once on that beat (ride, snare, hat-foot)", `chapin#4` "the ride is written as straight triplets for clarity". **Measured three ways.** Scope: parsing HEAD against the working tree per slug gives 217 slugs both sides, exactly 2 changed, and **zero prose or metadata changes anywhere in the corpus** — and normalising the diff by swapping the key back makes every added line **byte-identical** to the one it replaced, so no duration, dot, rest, tuplet, chord membership, accent or sticking moved. Geometry: all 58 hand noteheads at **y=58.000**, exactly the top line, still x-heads by SVG command sequence, round heads only at 73 (snare) and 93 (kick). Audio: 18 renders across both kits through the real player, **zero bit-identical** to their pre-change specs, with `jazz-ride-pattern#0`'s sub-1kHz energy fraction moving ~0.010 → ~0.175 onto a single-note ride probe. Two consequences stated rather than hidden: in the **acoustic** kit these nine get noticeably quieter (RMS roughly halves — the acoustic ride sample is quieter than the acoustic hat, a global property other lessons already inherit), and `chapin` Ex 5 now plays twelve ride strokes a bar at 75 BPM with a ride's decay where it played twelve short hat ticks. The fix also puts both lessons onto the site's **own documented convention** — `reading-101` says "hi-hat in the space above the top line, ride on the top line" — so `jazz-ride-pattern` and its own downstream lesson `jazz-medium-swing`, which teach the same pattern, no longer draw it on two different staff positions. **Filed BL-123, and it is the most important thing here.** Nothing in this repo could have caught this bug and nothing can catch a regression of it — mutation-proved, not argued: reverting all nine exercises leaves **every source-side gate green**, with controls that do fail correctly. And it is wider than ride-versus-hat: writing the chapin **snare** on the hi-hat line also passes all 20 gates. The blind spot is the class *"a note on a wrong but legal staff position"*, and today the only tripwire is a sentence someone happened to write. Chunk plan revised **2 → 3**: remaining are `three-limb-patterns` #0/#1/#4, `four-way-foundation` **#0–#4 (five, not two** — the later exercises continue the same hand voice under a graduation criterion reading "ride 8ths"), `independence-clave-foot` #1/#2/#4, then `independence-melodic-snare` (4) and `fusion-coordination-foundation` (1), both invisible to a word sweep because no individual tip says "ride". Eleven verb-sense false positives are confirmed false and must **not** be swept ("ride the offbeats", "this is how a ride cymbal swings"), and `three-limb-patterns` #2/#3 are deliberately the hi-hat. Gates: build 0 · **20/20** · beat-count byte-identical · `--gate` OK, 33 metrics, `integrity.lessonSlugHash` unchanged · staves read at 1280, at 3× zoom, and pinned in a true 390px iframe.
- 2026-08-15 · iter 75 · [quality] **The click's downbeat lands on 1 again after a meter change** [BL-101] — `scheduleAhead` accents `beatIndex % beatsPerBar`, and `setMeter` reassigned `beatsPerBar` while leaving `beatIndex` counting, so the accented "1" landed wherever the counter happened to fall. Measured on the built site: 6/8→4/4 at 100 BPM **1200 ms late**, 2/4→7/8 at 240 six clicks late, and 5/4→3/4 at 30 BPM **four seconds with no downbeat at all**. The dot row told the same lie — `1 2 3 4 5 (blank) 3 4 1`, and a jump from dot 4 straight to dot 2. Zeroing `session.beatIndex` on a real meter change puts the next *newly scheduled* click on 1: swept across the whole bar in tenth-of-a-beat steps, **50 of 50 positions** land the first new click on an accent, zero row/pill disagreements across 65 scenarios. **The guard on that reset is the part I briefed wrong.** I said "reset the phase" without qualification; `adoptTempo` calls `setMeter` on every press of every exercise tempo button and **817 of the 832 name 4/4**, the meter the dock is usually already in — so an unconditional reset would have restarted the bar under a reader in 4/4 pressing "Metronome 90" beneath a 4/4 exercise, turning a rare mid-bar accent into a common one. Guarded on `v !== session.beatsPerBar`, and comparing against the *session's* copy rather than the module-level one is load-bearing, since `beatsPerBar = v` runs three lines earlier and the other comparison is dead code. **Two more defects on the same control, found by measuring rather than by the item:** a click committed under the old meter fired into a shorter row, `beatDots[5]` was `undefined`, and the pill lamp pulsed **alone** for a full 88–96 ms flash — the dock disagreeing with itself in peripheral vision, which is the one thing the beat row exists to be trusted about; and `renderBeats` rebuilt the row without clearing the pill, leaving the lamp lit over a dark row. **The window this fix does not close, measured rather than glossed:** clicks already handed to the audio clock keep their accent — bound 500 ms, measured max 370 ms — and in **3 of 13** control changes that committed click is itself accented, so you hear the old "1", a short bar, then the new "1", 250 ms apart at 240 BPM and only in meters whose accents are two clicks apart. Filed as **BL-120** with the fix worked out rather than folded in: a truncated bar at the instant you press the control is legible, an accent four seconds out of place is not. **The adversarial pass blocked the new gate and it deserved blocking.** The harness was written to the **`_site` root** — which the protocol forbids explicitly and records as having already cost two ticks, since `audit-site` walks every `.html` and skips only a top-level `dev/`. The tempo scenario **asserted nothing**: deleting `session.bpm = bpm` left the gate green and printing *"a tempo change left the phase alone"* while the running click ignored tempo entirely. The header claimed *"every run is identical"* against six runs measuring **217/216/216/216/214/217** clicks — and the three-click tolerance was **removed rather than widened**, by comparing flashes against clicks that had time to fire. And assertion 10 bounded the window by the very constant it was checking. All fixed — **and the builder generalized the last one to a hole it had just made**, because satisfying another finding by reading `START_LOOKAHEAD` from source reproduced the same circularity, so a 0.4 mutation moved the harness's own expectation and passed; it caught that in its own audit and added a second independent budget. **Then removing the tolerance opened a hole where the flake had been, and a third pass caught that too.** Comparing flashes against "clicks that had time to fire" anchored on `states[states.length - 1].t` — but `sample()` only pushes when the state key *changes*, so that is the clear-time of the **last flash**, not the last sample. The anchor moved with the very flashes being counted: stop flashing and the anchor stops with it, so the rule could never fire. A mutant that keeps the pulse loop running and stops flashing after 8 beats produced **80 flashes against 213 clicks and exited 0** — the beat row dead in all ten scenarios, inside the check named for protecting it, and any exception thrown in `flashBeat` does exactly that. Uniform degradation was still caught; a clean stop was not. Re-anchored on each scenario's own end time, which does not move with the data: the tail variance stays gone *and* the mutant fails, so the two were never a trade-off. Three committed measurements were wrong and are corrected: the `renderBeats` pill-over-dark-row was "24 ms over four dark dots" and is **10–75 ms**; the clamp figure was "88–96 ms" and is **85–90 ms**, where 96 was not merely imprecise but *impossible* against a `setTimeout(…, 90)`; and "4 of 14 change events" is **3 distinct in-flight clicks across 13 changes**. The critic also **withdrew a measurement of its own** — its 56–80 ms came from two sample points at a resolution that inflated them, and its 5 ms sweep then reproduced the builder's numbers exactly. Mutation audit: 28 defects caught, 8 controls green, plus the clean-stop mutant added permanently. Filed **BL-121** (`setMeter` calls `renderBeats` *unconditionally*, above the guarded reset, so a same-meter handoff wipes a dot mid-pulse — a press 10–20 ms after a beat leaves the downbeat visible for **5 ms**, under one frame at 60 Hz, so the "1" can be dropped before paint; ~11% of presses at 80 BPM and ~32% at 240, on the commonest press on the site, and edit 3 above is precisely what hides it from the gate) and **BL-122** (`check-transport-reserve` is the last check still writing its harness to the `_site` root — the live landmine the protocol note is about, and the precedent this gate copied). Gates: build 0 · **20/20** · beat-count byte-identical · `--gate` OK, 33 metrics · `_site` root clean afterwards.
- 2026-08-15 · iter 74 · [content] **The lesson that teaches reading pointed at the one empty line on the stave** [BL-087] — `reading-101` told beginners the snare sits on the **middle line**. It sits in the middle *space*, third from the bottom — and `b/4`, the middle line, has **zero drum uses across all 852 exercises**, so the lesson whose whole job is teaching people to read notation sent them to the one position this site never writes anything on. Two more claims in the same paragraph fell with it: *"Toms sit between snare and hi-hat in pitch order"* is false for the floor tom (`a/4` at y 83, **below** the snare at 73), and hi-hat and ride were given as one position where the corpus draws the hat in the space above the top line and the ride on the line itself. `the-drum-kit` was correct throughout and was left alone — 20 claims including its hand-drawn diagram, whose SVG geometry the critic verified glyph by glyph. The rewrite states all nine positions and turns the empty line into a teaching point rather than a silence: *"No drum is written on the middle line itself, so the snare's space sits just above it."* **The sweep the acceptance actually demanded** — "no two lessons give conflicting positions for any voice" is a corpus job, not a two-lesson fix — found a third wrong lesson nobody had flagged: `rock-tom-grooves` said the activity "sits below" the snare on the high tom, mid tom and floor tom, where the high tom (63) and mid tom (68) are **above** it. Four more were fixed after the adversarial pass under a rule now written down: **a sentence is a position claim when it pairs "line" or "space" with either a concrete key or the verb "notated" in a notation context.** `metal-quarter-bass` said two kicks are "notated on the same line (`f/4`)" under its own *Notation Note* heading, where `f/4` renders in a space; `rock-cross-stick`, `hiphop-r-and-b-basic` and `transcription-method` each said cross-stick is "notated on the snare line". The bare possessive idiom — "the snare line", "a busy kick line", meaning a *part* — is correct English and all **53 instances across 34 lessons were deliberately left alone**. Six lessons against the 10-lesson cap. **I briefed a wrong constant and both agents caught it.** I measured from the raw path origin, got every note one staff position too high, and concluded the snare was a *line* note — which made `the-drum-kit`, the lesson that is right, look like the broken one. I caught that much with a ledger-line check and briefed a "+5 correction"; that was **still wrong**, because the offset is **+5.054 for round noteheads and −4.015 for x-heads**. A uniform constant fixes the drums and moves every cymbal **9.069 units — 1.81 staff positions** — taking the ride off the top line and turning `the-drum-kit`'s correct cymbal claims into errors. The builder refused it and derived both; the critic reproduced both to under 0.001 units against VexFlow's own ledger lines. Recorded in `discoveryNotes` with the full table. **The gate, and the first one here to survive an independent mutation audit:** `check-staff-positions.js` derives the table from rendered SVG every run — including the stave lines themselves, so a geometry change re-derives rather than silently shifting every claim — and asserts positions, ordering (a collective "toms" must hold for *every* member), that the middle line carries no drum voice, and a coverage floor. **35 claims across 7 lessons, floor 32**, printing its own calibration on every run. **22 mutations, 20 caught, 2 controls green, 0 silent passes**; two real holes found by its own harness and fixed — a clause naming a second voice after the position was skipped as ambiguous, and `b/4` was skipped by the middle-line rule precisely *because* it has no player voice, so a middle-line note sailed past the one rule written to catch it. The critic then ran **41 mutations of its own and found no undocumented hole**. Iteration 70 mutation-tested all 18 gates and rated **zero** as protecting what they claim; this is the first to clear that bar. **And the builder refused a false green rather than banking it** — a late run reported PASS where FAIL was expected, after the critic had signed off. It read the patch instead of reporting it: that mutation set centre = *ink top* + 5.054, and every notehead here is exactly 10.108 units tall for both families, so half the height **is** 5.054 and it lands on the true centre for every key. No defect was injected; green was correct and the harness had mislabelled its own patch. Filed **BL-118** (`playing-to-a-song#3` has `f/4` in **both** `hands` and `feet` on beat 1, so the kick is drawn twice and the sample fires twice — and being in `hands` it is the only kick of 2345 that renders stem-up) and **BL-119** (`reading-101` teaches 9 of the 11 positions the corpus draws, omitting china and bell, and never states the round-versus-x rule — the bell shares y 63 with the high tom and is distinguished **only** by notehead shape, so a reader taught by this lesson misreads 278 noteheads; a missing claim is invisible to the gate shipped here, which is exactly why it needs an item). Gates: build 0 · **19/19** · beat-count byte-identical · `--gate` OK, 33 metrics · passage read at 1280 and 390 and beside its own stave at size.
- 2026-08-15 · iter 73 · [ui] **The site stops scrolling sideways when you make the text big** [BL-069, BL-098] — at a 32px root (browser text at 200%, what WCAG 1.4.4 asks a site to survive) **108 of 810 measured cells scrolled sideways**: the homepage worst at **+208px**, `/genres/jazz-fusion/` +179, `/genres/latin/` +140, a lesson +91, `/rudiments/` +86. Roots 16 and 20 were clean everywhere, so every failure landed on exactly the readers who need the text large — and nothing in the build reported it, because a page that scrolls sideways still builds, still passes the metric gates, and still screenshots fine at a 16px root. **After: 0 of 810**, and 0 of 2700 on the critic's independent grid — 20 pages, widths 280→1599 with off-step values, roots 16→40 with off-step values, both themes forced explicitly. **The item named one mechanism and there were three.** `.ledger__title` already had `overflow-wrap: break-word` (it is an h2) and still pushed the page 208px out, because CSS Text 3 excludes break-word's opportunities from min-content — `min-width: 0` fixes that and only that. `.ledger-row__title` already had `min-width: 0`: its box was right and the *words* hung out. And nowrap outranks both — the subtle one being `.ledger-row__meta`, where `1fr` is `minmax(auto,1fr)` so the track floors at min-content: at 280/@40 the tracks resolved to 61.19 + 12 + 197.02 = **278px inside a 248px box**, the 197.02 being min-content rather than the 166.8px `1fr` share. **The adversarial pass blocked this once, on the one clause I had called load-bearing.** The first version shipped `.btn { flex-wrap: wrap }` and asserted *in capitals* that the default text size does not move. It moves: at 280px/@16 — the literal default — the homepage primary CTA went 248×75.5 → 248×108.25 with the arrow glyph **orphaned alone on a centered second line**, 1701 boxes changed, and `/metronome/` got a lone ↓ under the "O", across cells that had no overflow before *and none after*. The parity grid behind that claim sampled 320 and 390 at a 16px root; **the regression starts at 312** — an artifact of the spacing, 8px outside its own grid, which is the same class of unbacked in-repo claim BL-098 exists to remove, re-introduced inside the block closing BL-098. `flex-wrap` was also unnecessary (still 0 of 810 without it) and was dropped; the replacement grid is **380 cells and 695,961 box observations** with the grid *stated in the comment* instead of the word "zero" — 0 changed at roots 16/18/20, five cells changed at 22/24 and each is the fix working (two were already scrolling sideways; two had `.ledger__title` at 259.25px inside a 248px box, bleeding into padding while staying on screen where no gate could see it). The count was wrong too: **104 → 108** (26 core + 82 broad), where 104 was not reproducible from any artifact on disk. **Two more the builder found by turning the critic's standard on itself:** `.chip { flex-wrap }` was the wrong mechanism — the culprit reports `min-width: auto` — and swapping to `min-width: 0` cut the `/rudiments/` parity delta from **6736 changed boxes to 8** while subsuming `.family-index__chip`; and `.listening__eyebrow` was dead weight added by symmetry and never observed to overflow, now gone with the comment saying why so nobody re-adds it. Against that, `max-width: 100%` on `.btn` was challenged as inert and **survived** — remove it and `/metronome/` at 280/@40 overflows 17.7px — so the comment cites that cell rather than asserting the need. **The comment this item was about was not where I said it was:** it sits in TRANSPORT DOCK CAPTIONS and claimed "there is no sideways scroll at any width", a statement about the *page* the dock block had no standing to make and false when written. The dock is now exonerated by measurement — not a culprit in one of the 810 cells — and the new `SIDEWAYS SCROLL CONTAINMENT` block owns the real numbers, bounded in its heading to roots 16–40 rather than reading as an unqualified promise. **And it caught itself a third time, on re-review of the rework.** The corrected block quoted the root-48 failures as "10 cells, +23px on `span.chip`" — the critic's own numbers from the artifact it had reviewed *before* the rework, which two of the four rule changes had moved: dropping `flex-wrap` reopened `/metronome/` at that root, and moving `.chip` to `min-width: 0` took independence-chapin to **+46px on `a`** and removed `span.chip` as a culprit anywhere. Re-measured against the shipped sheet it is **20 of 300**. Two smaller ones with it: the stated grid didn't sum to its stated total (280→480 step 16 is 13 widths, not 14, and two wide cells were undisclosed — the arithmetic is now spelled out as 350 + 30), and three box counts were off by one from a sentinel key in the builder's map (**1336/1336/486 → 1335/1335/485** against the real HEAD blob); the unverifiable 695,961 now reads "roughly 700,000". **Three rounds, three classes of unbacked in-repo number, every one inside the package written to end unbacked in-repo numbers** — which is the honest lesson of this item rather than a footnote to it. Also worth recording: the critic **retracted its own finding** — it had called `max-width: 100%` inert, and every probe showing that still had `flex-wrap: wrap` present, masking the need. Filed **BL-117** (containment breaks at a 48px root in 20 of 300 cells — 300% text, outside 1.4.4 and both acceptances, named in-comment rather than implied handled; one cell still has **no culprit the detector can name**, so the detector has a blind spot and that gets fixed first; plus one artifact *inside* the promised band — at 280/@40 the `/metronome/` button is capped at 248px with 258px of content, so the arrow paints past the pill edge, the price of dropping `flex-wrap`). Gates: build 0 · **18/18** · beat-count byte-identical · `--gate` OK, 33 metrics · both CTAs re-screenshotted byte-identical to HEAD.
- 2026-08-15 · iter 72 · [content] **The wrong-beat campaign is closed, and its gate now reads 27% of the corpus instead of 6%** [BL-086 chunk 2/2, BL-100] — the harder half, where the target beat did not exist yet so rests and durations had to be rewritten: `hiphop-modern-production#3`, `hiphop-future-r-and-b#2` in 5/4, `hiphop-808-translation#0` (kick to the &-of-3 plus the snare move chunk 1 deliberately withheld), `four-way-foundation#4` and `#3`, `#2` fixed in the words rather than the notes because its own graduation criterion proves only **one** snare moves, and `rock-shuffle-rock#1`'s title. `audit-lessons` stayed **byte-identical** through all of it, which is the sharp edge when durations change. **The builder refused my instruction and was right.** I briefed a ride move from beat 2 to 1.5 on `jazz-broken-time-intro#3`; it declined, arguing the ride pattern is 1, 2, 2.5, 3, 4, 4.5 and never uses 1.5, and that the lesson's thesis is subtraction so bar 1 must be a *subset* of bar 2. I verified both — the move would have invented a note. **But the adversarial pass then found it had pinned the wrong side of the same page.** Exercise 3 was titled *"Skip Notes Only (No Quarters)"* while playing 2, 2.5, 4, 4.5 — and the site defines the skip note in `jazz-ride-pattern` as the second, swung 8th, so the skips are **2.5 and 4.5**. It was byte-identical to exercise 2, made the lesson's own three-step build a no-op, and falsified its graduation criterion; the tip written that round described a wrong stave accurately. The stave now sounds only the two skips, the three-step build is real (minus beat 3 → minus 1 and 3 → skips only), and the lesson body was rewritten too, because it described a quarter on beat 2 that the ride pattern does not contain. Being right about the refusal and wrong about which side to pin were independent, and only the adversarial pass separated them. **The gate this acceptance owed:** `check-tip-claims` went from **51 claims over 6% of exercises to 311 distinct claims over 227 of 852 — 27%** — with the unparseable exemption list emptied from 5 to 0. The critic found four ways the first rewrite was weaker than its headline and all are fixed: **13 of the legacy 51 claims had silently stopped being checked**, because exemptions were scoped to a whole exercise rather than a voice — `KNOWN_OPEN` had quietly retired a snare claim enforced since iteration 52; **a parenthetical after a position list discarded the claim**, so a flat lie ("Snare on 1 and 3 (right where the click is)" against a stave with 2 and 4) *passed*; `POS_RE` could not read **"on beat N"**, which 62 corpus tips use — meaning the one page where the brief had been refused in favour of prose was the one page the gate could not check; and the 250 headline was 199 distinct, with the floor set high enough to **block its own de-duplication**. A parity harness now asserts every one of the legacy 51 is still enforced: **51 of 51, zero lost.** **BL-100 closed with it** — `hiphop-808-translation#1`, titled "Floor-Tom-as-808 Sub Layer" and containing no floor tom, now carries `a/4` on all four kick notes so its tip's promise is true; the critic confirmed it is playable exactly as written and corrected the builder's justification, which had oversold a corpus count. **Scope recorded rather than hidden:** 11 lessons against a 10-lesson cap, four exercises outside the item's original twenty (each surfaced by the new gate and each verified genuinely wrong beforehand), and `latin-mozambique#1` — which *was* in the twenty — had been missed and undisclosed until the critic caught it. Filed **BL-115** (`jazz-comping-basics` counts a beat off across the whole lesson and says so in its own tip — a convention decision, not a slipped note) and **BL-116** (`funk-james-brown#0`'s kick claim needs the Cold Sweat transcription to settle, so it is the single remaining voice-scoped exemption). Gates: build 0 · **18/18** · beat-count byte-identical · `--gate` OK.
- 2026-08-15 · iter 71 · [quality] **The tuplet numerals are back on the page** [BL-111] — the gate said *"nothing a stave draws may fall outside its viewBox"* while **332 of 892 staves drew ink below their frame and 66 above it**, live on main, and it printed OK on every one. `rock-prog#0`'s two tuplet **"3"** numerals were invisible, so a reader saw two beamed eighth-note groups where the lesson means triplets. `fusion-half-time-shuffle#1` printed **3, 3, (nothing), 3** over four identical triplets — the missing one being the third, raised to clear an accent. That is verbatim the defect the gate was written for at iteration 31, reappeared on the opposite edge and hidden for forty iterations. **The cause was that the vertical half was a tautology:** the renderer derived the frame from `minY`/`maxY` with the same two regexes the gate re-ran, so it re-checked the renderer's arithmetic with the renderer's own measurement — the exact anti-pattern its own header names. It could not see `<path>` at all, which is what noteheads, stems, beams **and the tuplet numeral** are drawn as. Across 932 staves it had never once found ink outside a frame. The renderer now measures true ink — exact stationary points of every cubic and quadratic, sampled arcs, real rect and text boxes — and grows the frame on all four edges; the gate got an independent full-box parser on both axes that shares no code, constants or numbers with it, plus a left-edge check, `<line>` coverage, a loud failure on any `transform`, a dead-space check, and coverage asserted rather than printed. **Clipping 332/66 → 0/0/0/0**, re-measured independently three ways including **raster ground truth** — rendering at 4–6 device pixels per unit and finding the ink box in real pixels, worst slack 1.5u top and 2.0u bottom. **382 frames grew, 0 shrank, 0 changed width**, so `natW` is untouched and all 494 density floors are byte-identical — BL-040 undisturbed, verified against the pre-fix widths rather than assumed. Only block height moves, median +9.2%. And the change is **purely framing**: strip the viewBox and VexFlow's auto-ids and all 852 lesson staves are byte-identical old versus new, so grow-to-fit cannot be masking a placement bug. Mutation proof: the new gate exits 1 on the pre-fix renderer with `FAIL: 398`, and 8 of 16 renderer mutations turn it red. **Three corrections came out of the adversarial pass, one of them to my own number.** I reported 292 staves as independently verified; the real figure is **332**, because my parser measured path centerlines and forgot VexFlow strokes them — the same 0.75-unit inflation the builder had found and I had missed. Both headers now carry the gate's own numbers and say where they came from. The dead-space check would have false-fired on an author-pinned `spec.height`, turning a supported authoring move into a red build with a misleading message. And **438 `<text>` elements carry no `font-size`** — the open-hat "o" annotations, 14% of the corpus's text — with both parsers guessing 12pt where the real inherited value is 10pt: wrong identically, safe only because the guess was larger. Now both read the root. That correction also sharpened an honest number: **369 of 382** frames grew on measured geometry, and 13 grew on the text estimate alone and were never clipping. The header says so. Two things the header now states rather than implies: green means *nothing is clipped*, not that the geometry is right — disabling the arc sweep, the cubic stationary points or the stroke inflation each leaves it green, because the 4-unit pad absorbs anything under ~5u; and `MIN_STAVES` has zero headroom by design. Filed **BL-114**, found by the critic as one stray `&amp;` and re-sized by me to what it is: **130 double-escaped entities rendering across 51 pages**, mostly in exercise titles a reader scans — "Kick on the &amp; of 3" — with the trap recorded that 211 tips and 54 lesson bodies hold entities *legitimately* because they render as raw HTML. Gates: build 0 · **18/18** · beat-count byte-identical · `--gate` OK, no metric moved · the recovered numerals read at 2×.
- 2026-08-15 · iter 70 · [loop] **Discovery — I audited the auditors, and none of the eighteen protects what it claims** — every metric was flat again (33 of 33), so this pass went after the one thing nine iterations of adversarial review kept pointing at: critics had found blind spots in **five separate gates**, always the same shape. So the sweep mutation-tested all eighteen against their own headline claims — construct the defect the header names, then watch whether the gate fails. Roughly 180 mutations across four agents. **Verdicts: one passes-for-the-wrong-reason, sixteen partial, zero rated "protects what it claims."** **The worst finding is live on main right now.** `check-notation-frame` exists because tuplet brackets were once drawn at negative y and silently clipped, "so every shuffle lesson rendered without the 3 that is the entire point of the exercise". That defect is back on the **opposite edge**: **292 of 852 staves draw ink below their frame today**, 34 above it, and the gate prints OK on all of them. `rock-prog#0` has two tuplet "3" numerals sitting outside its viewBox — invisible on the shipped site — so a reader sees two beamed eighth-note groups where the lesson means triplets. `fusion-half-time-shuffle#1` loses the "3" over its *third* triplet, the one raised to clear an accent, so the page prints 3, 3, (nothing), 3 over four identical triplets. The cause is that **the vertical half is a tautology**: the renderer derives the frame from `minY`/`maxY` with the same two regexes the gate re-runs, then pads by 4 — so it re-checks the renderer's arithmetic with the renderer's own measurement, which is the exact anti-pattern its own header names ("never measure the output with the thing that produced it"). Across all 932 staves it has never found a single element outside the frame and the smallest slack it ever sees is precisely that 4-unit pad. The horizontal half, written at iteration 68, does it properly by parsing path geometry; it needs a y twin. Filed **BL-111**. The rest of the audit is **BL-112**: `check-exercise-meta`'s tempo regex cannot match `♩. = 80` **at all** — the dot sits between glyph and equals — so all 40 dotted-quarter metas are skipped entirely while being counted in its "all 852 match" line, which corrects the mechanism recorded for BL-102; its meter whitelist silently drops 8/8, 10/8 and 13/8, leaving `odd-meters-9-8#3` **100% unchecked**; and its "journey" escape disables the meter assertion on any meta containing a bare `+`, which about 13 of 18 exempt metas trip on ordinary prose. `check-beam-groups` examines **39 of 852 exercises (4.6%)** because it returns early unless a spec has tuplets. `check-tip-claims`'s exemption filter matches `sing ` **inside** phra-sing, clo-sing, pas-sing — 39 tips carry such a token and only 5 are the word. `check-exercise-titles` lets `slow-practice` ship three staves all called "Round 1" while its own tip says "20% faster than Round 1". And `check-accent-dynamics` cannot see the one constant its failure text warns about, because the accent gain is the master gain of both the shipped render and its control and cancels out of every ratio. **The first screen-reader walk of the site** (measured from the real AX tree over CDP, not inferred) found every one of **892 staves announcing only its title and tempo** — 77% contribute *zero* text nodes, so `first-beat#1` is, in full, "Drum notation: 1 — Hi-Hat Alone, Quarter Notes — 4/4 · ♩ = 80" with no children. **What saves it is the tips**, and the walker was emphatic: 685 of 685 grooves have one, they are largely prose transcriptions — *"hit on 1, 2, 4, 5, 6, leave 3 empty"* — and they could practice from them. Still filed as **BL-113**, because nothing tells that reader the tip is their substitute; they learned it by reading 892 labels. Also recorded: **`content-visibility` costs a screen reader nothing** — but only with `--force-renderer-accessibility`, without which `/rudiments/` reports 4 of 40 headings and the walker nearly filed a phantom blocker. Snapshot: 852 exercises · 448 listening entries · 217 lessons · 228 pages · **18 gates, ~40s to run all of them** · 33 metrics flat · integrity all 0. **113 items: 61 done, 49 ready, 3 blocked-human, none parked.**
- 2026-08-15 · iter 69 · [ui] **The header sheds by rule, and stops eating its own name** [BL-064, BL-092] — two items, one defect. The row was `logo + theme + pill + burger` with everything fixed except the logo, so **the site's own name absorbed every deficit** and clipped to **"drum.tov"** — with `text-overflow: clip`, so it read as a different wordmark rather than as truncation. And there was no rule for what gives way when anything is added: a previous addition had put all 228 pages into sideways scroll below 333px, patched by a one-shot breakpoint. There is now an explicit ladder, cheapest loss first: **the nav row folds into the hamburger → the pill's meter readout → the wordmark goes cleanly to the seal → the theme toggle moves *into* the menu panel as a labelled row.** The **hamburger and the clock pill never shed**, because below their folds each is the only route to something — site navigation, and the metronome. Driven by `@container` with `rem`-keyed thresholds, and the unconditional argument for that is one the first justification missed: **the row is capped at 1200px while the viewport is not**, so above 1200px a media query measures a box the row never gets. Thresholds are the row's own parts summed rather than numbers someone picked — the model predicts a 897px min-content at a 16px root and the measured value is **897.1**. **Fifteen broken cells of 56 went to zero** — twelve wordmark clips and three row overflows, and three of those were horizontal page scroll on all 228 pages that no gate had caught. **The adversarial pass found no blocker and verified far more than I asked:** 895 cells, including 648 CSS cells across twelve root sizes (eight the builder never used) and 247 state cells across menu-open, dock-open, no-JS, legacy-engine and both themes — the pill and hamburger held in every one, hit-tested at centre and both diagonal corners. It independently confirmed all three `@container` claims, including the load-bearing one: **inline-size containment does not hide overflow from `documentElement.scrollWidth`**, so the existing gates stay awake. **What it caught was real:** the fit guard listened to the window but to nothing inside the row, so changing the stored tempo from 80 to 240 — what the dock does when a reader drags the slider — left the row **5px over with no shed classes** until the next resize. My prescribed fix was wrong too, and the builder showed why: a `ResizeObserver` on the row alone cannot see it, because the row is block-level and its own box never changes when a child grows inside it. It observes the row **and every element child**, so a future control added to this row is observed by construction rather than by someone remembering. Also fixed: the guard stopped at `clientWidth + 1` while the gate failed above `clientWidth`, so a guard could not always satisfy its own gate; and `check-nav-a11y` now asserts the menu theme row's label, its agreement with the header toggle, and that the text **changes on activation** — mutation-proved, because without it a broken label sync would leave every dark-theme reader a wrong label with all 18 gates green. **An honesty correction the critic made and the builder accepted against its own headline:** those 15 cells were measured by setting `html { font-size }`, which models Firefox's minimum-font-size, user stylesheets and devtools rather than "a reader at 150% text" generally — because this site sets `html, body { font-size: 1rem }`, and `1rem` on the root *is* the browser default, so a reader changing their browser font setting moved the old thresholds with them. The cells are real and the fix strictly better; the population is narrower than first claimed. Two more found by measuring: a **`--header-h` feedback loop** where JS wrote the measured height into the token that was also the row's `min-height`, growing the header **1px per resize** (63 → 122px over 60 passes), now flat; and the header **exonerated** for the remaining raised-text page overflow — zero header culprits at every one of 648 cells — so `h2.ledger__title`, `.listening__count` and `.lesson-row a` are named with numbers on BL-069 and BL-098 instead of a general complaint. Gates: build 0 · **18/18** · beat-count byte-identical · `--gate` OK · headers read at 280/390/1280 × 16px/24px roots, both themes.
- 2026-08-14 · iter 68 · [quality] **The dense phrases were never drawing four clean bars** [BL-065] — the item said eight multi-bar staves drew past their frame. They did. The frame was the least of it: the same eight carried **95 note-on-note collisions across 42 internal barlines**. On `hiphop-questlove#3`, bar 1's last four sixteenths were drawn **on top of** bar 2's first four — 90.7 units, about three and a half noteheads — so that barline printed a run of **eight X-heads at half spacing under two superimposed beams** where four belong. On `rock-dynamics#1`, an **accent lesson**, the collisions printed **doubled `>` marks that do not exist in the spec**, so a student reading accent placement off that page read the wrong pattern. Clipping was hiding the fourth instance of a defect already visible three times over. **The first build fixed the frame and I nearly shipped it.** One line, no re-render, byte-exact: eight viewBox attributes changed, zero glyphs moved, every gate green. Correct code encoding the wrong decision — it fixed **zero** of the 95 collisions and turned three invisible notes into three orphaned ones past the closing barline. The adversarial pass measured the collisions and refuted the premise I had handed the builder; my framing of "a clean phrase missing three notes versus four bars plus an orphan" described a page that has never existed. **The renderer's own comment had ruled the real fix out on numbers that were wrong** — it said widening "does not converge" (it converges in **2–14 rounds**), that the fixed point lands at 1700–1800 (measured **1043–1802**), and that it costs a flat ~22% (measured 20.4–22.3% on seven and **4.1%** on the eighth). That comment is why these eight survived dozens of iterations: it read as settled. **Shipped instead: generalized width convergence** — the retry loop lost its sticking-only scope, depth 3 → 20, a hard 2400 ceiling, and an overflow yardstick of `max(drawOverflow, rectOverflow)` measured *after* beams and tuplets draw. **Collisions 95 → 5**, seven of eight completely clean. Zero staves overflow, zero hit either cap, the viewBox backstop fires **zero** times, and the frame-to-ink gap is now **7.00 on all 932** with a `<rect>` as the rightmost ink every time — the same gutter every other stave on the site has. Blast radius identical to the safe version: **924 of 932 byte-identical, exactly the same 8 changed**. `min-width` unchanged on all 892, so BL-040's density work is untouched and spacing stays at its derived 28.0px per event; what shrinks is glyph size, 12.8 → 10.1px, and none of the eight carries sticking so PKG-7's 10px floor is not in play. **A second bug fell out of it:** `drawOverflow` did not bound tuplet-bracket ink while its comment claimed it did — brackets are bare `<rect>` end-ticks attached to no tickable, and a phrase whose closing tuplet ends in ghost rests put **13.91 units outside the frame**. Twelve real corpus tuplets already end in a rest and were safe only by luck. The new gate catches it, proved by a mutant that **fails at a converged width** — which is what shows `rectOverflow`, not convergence, is the fix. That gate parses path data independently rather than reusing the renderer's own number, which is exactly why it could catch a bug in the number the renderer widens by. Filed **BL-110**: one bar still collides, because every bar gets `innerWidth / bars` regardless of how many events it holds, so no total width can serve a dense bar next to a sparse one — and the frame check is structurally blind to ink that is inside the frame but on top of another bar. Build 3.4s → 5.6s. Gates: build 0 · 17/17 · beat-count byte-identical · `--gate` OK · three staves re-rendered from the shipped build and read.
- 2026-08-14 · iter 67 · [content] **A sticking letter names a hand again** [BL-078] — the corpus had drifted into using lowercase sticking to mean *ghost* instead of *left hand*, and `finger-control#2` proved it inside one sentence: *"Hi-hat 16ths in the **right** hand, snare 16ths in the **left**. The **r** snare hits are all fingers."* Those are left-hand strokes marked `r`. Real notation names the hand with R/L and marks a ghost with dynamics or a bracket — never with the case of the letter — and this site teaches reading, so the private convention was miseducation. **Decision: the letter names the hand and nothing else.** 101 lowercase letters corrected to the hand that actually plays them, **114 explicit `ghost: true` markers** across 13 exercises in 5 lessons, `player.js` now reading the marker instead of the case, 8 tips rewritten, a new gate, and the rule written into `docs/content-style-guide.md`. One carve-out, now deliberate and documented: the flam/drag **grace** note stays lowercase, because that is standard rudimental spelling and six lesson bodies teach it by name. **The best result was one nobody was looking for.** `ghost-notes-found#1` — *"Sixteenth-Note Ghost Stream on Snare"*, whose tip asks for "a soft hum" — and `finger-control#0` were playing **sixteen full-volume strokes each**, measured at 1.000. They now measure **0.250**. Two ghost-note exercises that had never had a ghost. `funk-sixteenth-feel#2/#3` improved 0.500 → 0.250 as well, while `paradiddle#0` correctly holds at 0.500 and `funk-ghost-notes#0` at 0.250. **The adversarial pass held the change up and it survived the hard parts:** all 114 markers checked against their own tips with independent position arithmetic — sum exactly 114, no marker on a note a tip calls loud, no unmarked note a tip calls a ghost; all three "continuous hat ⇒ left hand" derivations verified from the key arrays rather than the prose; and **no hand assignment changed anywhere**, case-folded old against new. It also confirmed corpus-wide that **exactly one note got louder**, the one the builder had already named. **What it caught mattered though.** `dynamic-spectrum#3` promised *"four dynamic levels"* and now sounds **two** — the fix put piano above the ghosts and collapsed it into mezzo, permanently, because the player has three tiers and two are spent on forte and fortissimo. The builder had written that into a source comment no reader sees while leaving the tip promising four; the tip now says what the page and the playback can actually deliver. The style guide printed `lR` as a flam spelling that **the new gate rejects**, so an editor following it would write an illegal value. And its hand rule — "R on the hat, L on the snare" — was **falsified by two exercises in the same commit**, where the hat is played by both hands alternating, so it is now two named models with the requirement that the tip say which is in play. **And the gate that checks all this could be emptied silently:** deleting all 114 markers printed *"0 ghost markings all well-formed"* and exited 0, with no case anywhere covering a zero-accent spec — so the exact bug this item was filed for could be reinstated with every gate green. There is now a `noaccent` case measuring 0.250 ×16 on a spec that accents nothing, mutation-proved to fail when the markers are stripped, and the check's header says plainly that it enforces well-formedness rather than completeness. **A correction to the record:** the builder rejected parenthesized ghost noteheads as impossible; `new VF.Parenthesis()` defaults to LEFT so a bare constructor draws only the opening bracket, but passing both positions draws complete ones — rendered at the site's real geometry with **zero frame overflow**. Its conclusion survived on its second reason only, that bracketing 114 of ~750 ghosts would invent an unevenly-applied convention, which is this item's own failure mode. Filed as sequencing (**BL-107**), with **BL-108** for the dynamics lessons that teach five levels against a player with three, and **BL-109** for three tips that assert a right hand the stave never letters. Also mine, for the two exercises where every note is a ghost and no accent can mark them: their metas now print `pp throughout`, the convention `rock-dynamics` already used. Gates: build 0 · **17/17** · beat-count byte-identical · `--gate` OK, no metric moved · 13 changed staves read at size and at a true 390px.
- 2026-08-14 · iter 66 · [ui] **Four presses to the first exercise, down from seventy-seven** [BL-073] — a keyboard persona needed **69 Tab presses** to reach the first exercise control and **228** to cross the homepage, where 217 of 234 stops were consecutive ledger links and only 3 of 19 chapters had any jump target. A measurement panel walked four page types in a real browser, three designers proposed independently, and a judge picked a hybrid after testing all three mechanisms in Edge and catching that the winner's own CSS would fail the metrics gate. Shipped: a focus-revealed **skip rail** (`Skip to content` · `Skip to exercises` · `Jump to a chapter` · `Skip to footer`), `#exercises` on every lesson, a 19-link chapter panel that costs **0 tab stops while closed**, and `aria-labelledby` naming all 19 chapter regions. **Measured, and re-derived independently across all 217 lessons at both widths: first exercise control 77 → 4 presses, exactly 4 on 217 of 217; first Play 6 (or 5 on the 14 lessons whose first exercise has no tempo button); first footer link on the homepage 227 → 5; any of 19 chapters within 23.** Cost: **+1 to +2 tab stops per page**, verified in every state including a returning visitor. **Three defects found by measuring, all older than the item.** The site's skip link had been **lying for 70 iterations**: Enter set the hash but `activeElement` became `<body>` because `#main` had no tabindex, so it worked for sighted Tab users and silently failed the screen-reader users it exists for. The rudiments family chips and one in-prose link I added myself in iteration 58 were dead the same way. And **Escape-closing the mobile menu permanently removed the only sub-720px navigation from the tab order on all 228 pages** until reload, because `setNav` stamped `tabindex="-1"` on a `<button>`. All three fixed; 698 in-page anchors across 228 pages now resolve to something focusable, enumerated by a new gate. **The adversarial pass found a blocker I would have shipped:** at a true 390px the chapter panel covered **100% of the focused control** — all five hit-test points, no focus ring anywhere on screen, three consecutive stops fully hidden — with **no dismiss at all**: Escape left it open, an outside click left it open, and `#chapter-index` is a shareable URL that landed a visitor with 93% of the screen covered and the header unclickable beneath it. WCAG 2.4.11. The prescribed fix was a close link; the builder added it, watched its own new assertion **still fail**, and made the panel **in-flow** instead of fixed — nothing can be behind it because it pushes the page down. Now **0 of 23 stops obscured** at 390×667, 390×844 and 1280×900. **And the gate was enforcing the mechanism while asserting no digit of the bound** — `seq.indexOf(ae)` was always −1 because every rail target carries `tabindex="-1"` and the tabbable list filters those out, so all 20 rows returned null and nothing read them. A template edit reinserting 60 stops would have passed all 16 gates. It now commits the bounds as failing assertions, plus two re-inflation budgets — one of them literally the shape of iteration 64, which moved `/rudiments/` from 112 to 152 stops with nobody watching. **`css.outlineNone` 0 → 1, deliberately and with the baseline regenerated in the same commit**: `main:focus-visible` painted a 2px accent rule edge-to-edge across every page, floating in the 96px gap above the footer, pixel-probed at rows 427–428 on `/404` and y=762–763 on a lesson after paging down. It is replaced by `box-shadow: inset 0 3px 0`, a single bar under the header — an indicator replaced, never deleted, which is the one legitimate use of that declaration. The builder's first attempt read 0 → **2** because its own explanatory comment contained the literal string; it reworded so the metric counts the declaration and not the mention. 23 mutants across two rounds, 23 caught. Filed **BL-104** (the sidebar lives inside `<main>`, so the *primary* skip link still lands 69 stops from the lesson — the headline is only true of a reader who ignores the link they know), **BL-105** (the losing disclosure design had far better numbers, 234 → 36 stops, and is the owner's call), **BL-106** (19 public fragment URLs derive from curriculum slugs with nothing pinning them). Gates: build 0 · **16/16** · beat-count byte-identical · `--gate` OK post-rebaseline, one key moved.
- 2026-08-14 · iter 65 · [quality] **You can finally hear the ghost notes** [BL-097] — the word `accent` did not appear anywhere in `player.js`, so a whisper and a backbeat came out at identical volume on a site whose premise is real notation *and* audio. Iteration 61 made the stave print `>` on the backbeats and leave the ghosts bare; the lesson then had to admit in prose that *"the player still fires every snare note at the same volume, so the 4-to-1 ratio itself is yours to supply."* That sentence is now false and has been rewritten — in **both** lessons that carried it, searched rather than assumed, because iteration 61 shipped exactly that mistake by fixing one sentence and missing its twin. One `GainNode` per (note, key) between the voice and the master, created only when the level is not 1, so all **26** voice functions are untouched and an accent-free spec builds byte-for-byte the graph it built before. Three tiers: accented notes and any voice the spec never accents stay at **1.0** — nothing is boosted, so "no exercise gets louder" holds by construction; an unaccented snare or tom of an accented voice is a **ghost at 0.25**; an unaccented cymbal or kick is a **tap at 0.5**. Measured with `OfflineAudioContext` against the shipped `data-spec` on the built page: attenuation lands on exactly **0.250 / 0.500 / 1.000**, giving **4.00-to-1** on `funk-ghost-notes#0` electronic and **3.97-to-1** acoustic. **The adversarial pass caught a real blocker by checking a field neither the builder nor I had looked at.** The builder applied 0.25 to all 190 exercises that lower a drum note, defending it with the claim that `paradiddle#0` and `ghost-notes-found#2` have structurally identical specs so nothing readable could separate them. They are not identical — the **sticking case** separates them, and the corpus says so in the very exercise cited: *"beats 1, 2, 3, 4 are full strokes (uppercase) and the e / & / a positions are ghosts (lowercase)."* **62 exercises were whispering at 4-to-1 while the letters printed under their own staves called those strokes full** — the whole accent-tap shelf, and the most-trafficked rudiment lesson on the site, where the tip asks for "*slightly* louder". An uppercase-sticking veto sends those to 0.5: `paradiddle#0` went 5.24 → **2.62** and the four-level dynamics ladder got its steps back, while both ghost lessons and both rewritten sentences stayed exactly where they were. The builder then corrected **me** on a detail — I called `stick-grip#2` a third miscased exercise and it is not, being titled "Loud R, Soft L (Accent / Tap Demo)" and asking for taps. **A claim that had to be weakened rather than defended:** both comments said nothing anywhere could come out louder. False — samples are signed and the mix is a linear sum, so attenuating one of two partially-cancelling contributions can raise a peak; four exercises do, and the worst note-window rise is 1.0187. What is actually invariant is **RMS, which fell in 201 of 201**, so the gate asserts that instead. That measurement also found a bug in the gate itself: its tolerance was 1.002, *below* the 1.019 cancellation ceiling, so it would have failed correct code the moment a case covered it. An unclaimed win: **196 of 201 of these exercises already clipped past full scale** (up to 2.172) and this reduces it — `latin-mozambique#1` 2.094 → 1.487. New gate **`check-accent-dynamics.js`**, the fifteenth and the first that can hear anything: nine offline renders across both tiers, four voices and both kits, each against its own accent-blind control so no baseline file is needed; ten mutants run, nine caught, and the tenth documented as a named blind spot rather than papered over. Filed **BL-103** (some accents mean a *timbre* or a *pitch* — a rim shot, hearing both pulses of a polyrhythm — and the player now plays all of them as volume; `reading-syncopation#4` would sound 10 of its 13 notes at a quarter, in a lesson about counting) and raised **BL-078 to P1**, because sticking case is no longer a printed letter: it now decides how loud a note plays. Gates: build 0 · **15/15** · beat-count byte-identical · `--gate` OK. Nobody has actually listened to any of it; every claim above is a number.
- 2026-08-14 · iter 64 · [ui] **One tap to practice an exercise at its own tempo, and the page stops jumping when you close the dock** [BL-076] — the beginner persona measured **eight actions** to match the click to the exercise in front of them: tap the BPM box, clear "80", type 9, type 0, dismiss the keypad, press play, on a site whose headline is *"The whole town practices to the same clock."* Every exercise card now carries a third control reading **`METRONOME 80`** that sets the dock's tempo and meter and opens it without starting anything — a handoff the reader **asks for**, never an adoption, so a deliberately-set 60 is never overwritten by scrolling past something faster. **The scroll jump was diagnosed by bisection, and both my guess and the persona's were wrong about the mechanism.** It was `[id] { scroll-margin-top: calc(var(--header-h) + 12px) }` matching `#metronome-pill`, so the `focus()` at the end of `setCollapsed` ran scroll-into-view against a scroll box grown 74px *upward* — and it only showed on the chevron path because a pointer press on the pill focuses it first, making `focus()` a no-op. So the asymmetry was the no-op, not the focus. The number is 65 at 390 and 60 at 1280 because `chrome.js` overwrites `--header-h` with the measured 62px and the pill is 44px tall on a phone against 34px on desktop; the persona reported the narrow case, to the pixel. Mid-page deltas are now **0** for the chevron, the pill and Escape at both widths; the residual at maximum scroll (76 and 140) is exactly the body reserve the page loses (64+12, 128+12) and is identical for all three and for a bare class toggle. Every in-page anchor was re-measured on three pages at two widths: 10.6–11.5px clear of the header. **The adversarial pass found two blocking errors in the handoff and both were real.** Seven exercises at 250–280 BPM named a tempo the dock clamps to 240 — a button reading `METRONOME 280` that silently delivers 240, which would have been the fifth false affordance in this one component. And all **53 x/8 exercises handed the dock a bar twice the notated bar**, because `spec.bpm` is a quarter-note rate while the dock's bpm is *clicks* per minute and its beat count is *clicks* per bar: 6/8 at 80 is a 2.25s bar in the dock against 1.5s printed. Fixed by **omitting** the button on all 60, after rejecting three alternatives — tempo-only (a quarter-note click cuts across the dotted-quarter pulse and helps nobody), `bpm × denominator/4` (musically exact but prints a number contradicting the exercise), and `beats = numerator/2` (barline lands right, intermediate clicks are a 3-against-2, and the dock labels 3 as "3/4"). A correct absence beats a wrong presence here. 832 of 892 keep a button; the new gate **`check-tempo-handoff.js`** (14 checks now) pairs each one to its play spec and reads the dock's meter table *and* bpm range out of the built dock so the shortcode and `base.njk` cannot drift — mutation-tested 8 for 8, including widening the dock's own `max` to 300 and watching it demand the buttons that were correctly omitted. **The cost is measured, not waved at:** +52px per card at ≤414px, +41px in a second band from 560 to ~1100 where the control row drops under the title, and one tab stop per exercise — which moved **BL-073**'s own measurement (`/rudiments/` 112 → 152 stops) and raised it to P1, because leaving a filed defect's number wrong is not acceptable. Filed **BL-102**, the biggest thing this iteration turned up: **44 exercises play at a tempo that contradicts the tempo printed on them** — 41 marked `♩. = 80` that the player reads as a plain quarter, so the audio is 50% slower than the marking directly above it, and 3 marked `♪ = 160` that play twice too fast. `check-exercise-meta.js` cannot see any of it because it compares the last number without reading the glyph, and silently skips all 40 dotted-quarter metas. Also filed **BL-101** (changing the meter mid-click resets the accent phase, so "1" lands mid-bar). Gates: build 0 · 14/14 · beat-count byte-identical · `--gate` OK · scroll deltas re-measured after the CSS change · both themes read.
- 2026-08-14 · iter 63 · [content] **Nine exercises now play what they say** [BL-086 chunk 1/2] — twelve notes moved across nine exercises in nine lessons, each one shifting a voice from the beat the notation had it on to the beat the exercise's own title and tip say it is on. `independence-chapin-method#2` is titled *"Single Snare on Beat 4"* and promises "three things at once on that beat"; the snare was on **beat 3**, so the unison it exists to teach happened nowhere — snare, hi-hat and hat-foot now strike beat 4 together, and you can see it on the stave. `funk-displacement#3` promised a backbeat "one 16th off the home position" and sat exactly on 2 and 4 — a hash of every spec in the corpus had already caught it as byte-identical to seven plain backbeats. `rock-double-time#1` told the reader "the snare doesn't land on the downbeats" while landing on them. `fusion-broken-time#2` now has **all four clauses of its tip true at once** for the first time. Every edit is a key move between two slots that already existed, so no duration, dot or rest length was rewritten anywhere — verified by three corpus-wide invariants: zero duration changes across all 852 exercises, an identical key multiset inside each touched exercise (a move cannot create or destroy a voice), and only the intended nine changed at all. **And those invariants are exactly what let a blocking bug through.** The critic found that in `rock-prog#1` the snare moved to the & of 1 and its `accent: true` **stayed behind on the bare hi-hat at beat 2** — drawing an accent over a hi-hat on the one beat whose tip says *"not on 2 and 4 at all"*. Both my invariants reported clean, because an accent is a non-key property and its position sequence was byte-identical. Fixed, with the lesson recorded for the remaining chunk: a move must carry the note's other properties with it, or decide not to on purpose. Three follow-ons: `rock-prog`'s graduation criterion still named the old second hit; the chapin tip called beat 4 "the ride quarter" when beat 4 is an eighth; and `hiphop-trap#2`'s vacated beat 4 now draws a **visible** rest instead of a silent 120-unit gap. I rejected the critic's alternative there, which would have moved the hi-hat itself from 4.5 to 4 — no text asks for that. **A claim I refused to take on trust:** the critic reported that `accent: true` renders no ink anywhere, censusing the class `vf-articulation` at zero across every page, and concluded BL-085 chunk 1 shipped a no-op. Rendering one spec with and without accents differs by **1,272 bytes and exactly two `<path>` elements**, and the built page matches the with-accents render — VexFlow 4 simply doesn't tag articulations with that class. Recorded in `discoveryNotes`: don't census VexFlow by class name, count paths or look at the picture. Filed **BL-099** (19 exercises across 8 lessons say *ride* and notate a **hi-hat**, including `jazz-ride-pattern#0` "Quarters on the Ride" and all five Chapin exercises whose lesson premise is "lock the ride and hi-hat foot into their jazz roles") and **BL-100** (`hiphop-808-translation#1`, titled "Floor-Tom-as-808 Sub Layer", is a byte-identical duplicate of #0 with no floor tom in it). Chunk 2's diagnosis was also corrected: `four-way-foundation#4` needs a different note moved than recorded, and `#2` looks like a fix-the-words case where moving the note would cement an error. Gates: build 0 · 13/13 · beat-count byte-identical · `--gate` OK, no metric moved · staves read at 2×.
- 2026-08-14 · iter 62 · [quality] **The click survives the walk to the next lesson** [BL-091] — a simulated user set 90 BPM, pressed play, tapped through to the rudiment they wanted and got silence; they called it the difference between a practice tool and a demo, and said they would have opened their phone's metronome app instead. The honest fix is **not** continuous audio: browsers block an AudioContext without a user gesture on the current document, and the click that navigated belongs to the previous one, so any "resume automatically" would work on some machines and fail silently on others. Instead the dock now **arms itself**: the flag rides in sessionStorage, the dock comes back open with your tempo and meter intact, the play button wears a brass ring and the name *"Play, resume metronome at 90 BPM"*, and a line reads **"Paused. Press play."** One tap. The `/metronome/` page stops promising the sound follows you and explains the limitation in a sentence. **The critic caught a blocker that would have shipped and made the feature worse than nothing.** Nothing listened for `pageshow`, and every page here is bfcache-eligible — and because an armed page holds *no AudioContext at all*, a Back navigation restored the document verbatim and the arming code never re-ran. Two states resulted: a **missed arm** (idle dock, flag still set — the exact pre-fix behavior, now intermittent, which is worse than consistent) and a **stale arm** (a restored page still reading "Paused. Press play." when nothing was paused — a fresh false statement in the component whose last three fixes were all false statements). The critic could not reproduce it, because headless Edge ships bfcache off; the builder then turned it on and **proved both directions with real restores**, including audio scheduling after the press. Three more fixes in-tick: collapsing an armed dock now **clears** the flag, because before this a reader who had finished practicing had no dismissal that survived one navigation and would have fought the dock open across 217 lessons — while their collapse press durably flipped the very preference the override existed to protect; the status line gained a clamp, because at a 20px root it silently swallowed the word *"play."*; and three comments that stated wrong contrast figures were corrected against the real token values. **One pre-existing bug fixed because the new copy would otherwise lie about it:** on a Back navigation that *misses* bfcache, the browser re-applies its own form state after the script restores from localStorage, so the dock displayed 4/4 while the beat count, the dot row and the accent pattern were all 3. Five `autocomplete="off"` attributes. Filed **BL-098**: at a 32px root, 9 of 39 measured cells scroll sideways by up to 211px — identical armed and idle, so page-level and pre-existing — while a CSS comment claims a 32px root produces no sideways scroll at any width. Gates: build 0 · 13/13 · beat-count byte-identical · `--gate` OK · dock 129/65px with 11px clearance at every root, armed and idle. **Noted for the loop: `check-transport-reserve` passed this change blind** — its harness never sets the flag, so none of its 36 measurements ever applied the armed class.
- 2026-08-13 · iter 61 · [content] **The ghost-note lessons now show a ghost note** [BL-085 chunk 1/7] — 18 exercises across 9 lessons gained accents on the notes their own words call loud; corpus-wide, exercises carrying any accent went **235 → 253**. The flagship case: `funk-ghost-notes#0`, the first exercise of the lesson *about* ghost notes, whose tip asks for "a 4-to-1 ratio between accented and ghosted snare" while rendering its two backbeats and three ghosts as **five identical noteheads**. It now prints `>` on 2 and 4 and leaves the ghosts bare. `funk-the-meters#1`, whose tip says "only the snare on beat 4 is loud", now carries exactly one accent, on beat 4. **The adversarial pass earned this iteration, and the lesson is a process one.** My first 12 edits were chosen exercise-by-exercise from tip text **without checking the other staves on the same page**, which left pages half-marked and manufactured three fresh contradictions of precisely the kind this item exists to remove: `hiphop-boom-bap`'s body still read *"The notation cannot show you that part. It prints six identical snare notes"* directly above a stave that had just started showing it; `hiphop-neo-soul` 1 and 2 are byte-identical bars and only the first was marked, so the accents read as a dynamic instruction to *stop* accenting; and `funk-clyde-stubblefield` 3 is exercise 2 plus an open hat, under a tip promising the delta is one symbol, where the notation would have shown one symbol appearing and two vanishing. All repaired in-tick by widening to the siblings — **future chunks select by page, not by exercise.** Two exercises were deliberately left alone rather than guessed: `latin-samba-feel#3` (its tip also calls the pattern cross-stick, which the data model cannot express) and `funk-the-meters#3` (a two-bar broken backbeat whose bar 1 has *no snare on beat 2*, so the pattern that fits every other two-bar case does not apply). **The uncomfortable measurement:** this class has **no gate at all**. `check-tip-claims`'s regex overlapped **zero** of the 12 — every one phrases the claim differently — `audit-lessons` is blind because accents do not change tick arithmetic, and the string `accent` appears zero times in `audit-site.js`. The guard the acceptance calls for is deferred to the chunk that finishes the campaign, where it can land with a not-to-grow baseline. Filed **BL-097**: `player.js` never reads `accent`, so a ghost and a backbeat still come out at identical volume — the notation now tells the truth and the audio does not, and the ghost-note lesson says so in as many words. Gates: build 0 · 13/13 checks · beat-count byte-identical · `--gate` OK, no metric moved · staves read at 2× and at a true 390px.
- 2026-08-12 · iter 60 · [loop] **Discovery — +12 items, and the loop's own gates found nothing at all** — every metric identical to baseline (33 of 33), so the pass had to go where no gate can look. Three lenses did. **The mechanical half found almost nothing, which is itself the result:** across all 228 pages there are zero dead links, zero dead fragment anchors, zero orphan pages, zero dangling `aria-labelledby`/`for` references and zero duplicate element ids; `rudiments.js` — the file every audit tool skips — passes audit-lessons' own arithmetic over all 80 specs and 1,044 notes with zero beat mismatches, zero unknown durations and every `meta` string agreeing with its spec's meter and tempo; and the corpus is genuinely written rather than copy-pasted, with zero near-duplicate lesson pairs and zero verbatim shared paragraphs. **The content lens found the real haul.** Twenty exercises put a named voice on a different beat than their own words say (**BL-086**), and I re-derived the three severest from the specs myself: `independence-chapin-method#2`, titled *"Single Snare on Beat 4"* and promising "three things at once on that beat", has the snare on **beat 3**, so the unison it exists to teach happens nowhere; `funk-displacement#3` promises a backbeat "one 16th off the home position" and sits **exactly on 2 and 4**; `rock-double-time#1` says "notice the snare doesn't land on the downbeats" and lands on them. Two of those were corroborated from a completely different direction — hashing every spec put them in a **group of eight sharing one byte-identical plain backbeat**, so the duplication is the mechanism: a reference spec was reused where a displaced one was needed. **`reading-101` teaches the wrong staff position for the snare** (**BL-087**) — "the middle line", where lesson 1 says "the middle space" and the rendered geometry agrees with lesson 1. The two foundational lessons contradict each other about the most-used voice on the site, in the one lesson whose errors propagate into every later one. Also filed: notation that omits the very dynamics it exists to teach (**BL-085**, reached independently by two sweeps), ~22 titles claiming bar counts the stave lacks (**BL-088**), a sticking that says it alternates and never does (**BL-089**). **The persona walk** — six years in a covers band, phone, wants the rudiments and a click — found that **28 of the 40 rudiment cards are dead ends** (40 cards, 12 lesson links, verified) while the page tells you to "work outward" from #1 into a spine that is all dead ends (**BL-090**); and that **the click dies on every page navigation** (**BL-091**), which they called the difference between a practice tool and a demo, on a page that promises the metronome "follows you". Plus the wordmark clipping to "drum.tov" at 150% text (**BL-092**), notation being the one thing on a card immune to text zoom (**BL-093**), and three smaller ones. They also said the dock is now the best-labelled thing on the site — every control wears a word — which is iteration 59 landing. **Four alarms measured their way out of existence before becoming items**, and they are recorded in `meta.discoveryNotes` so nobody files them a third time: the rudiments page is 906 KB of raw HTML but **72 KB over the wire** under brotli; a duplicate-detection key that omitted `articulation` made the open-hats lesson look as though it notated no open hats; a ghost-note rule hunted a property the corpus does not have; and an accent rule matched the *negation* in titles like "No Accents". Snapshot: 852 exercises · 448 listening entries · 217 lessons · 228 pages · 13 gates · 235 of 852 exercises use `accent:true` · 60 use tuplets · integrity all 0. **96 items: 53 done, 40 ready, 3 blocked-human.**
- 2026-08-12 · iter 59 · [ui] **The transport stops lying about itself, and starts using words** [BL-074, BL-075] — both were the beginner persona's, and she named the first as the moment the site made her feel stupid: *"I tapped a play triangle and nothing played — it opened a drawer with a second, identical play triangle."* Three triangles were on screen at once. The pill's lamp now carries **no glyph at all** — ring = stopped, filled = running, brass offbeat, brick downbeat — and the disclosure is a CSS chevron driven off the `aria-expanded` the button already publishes, so there is no second source of truth in JS. `■` was as dishonest as `▶`: pressing the pill neither starts nor stops the click. The second item was worse in a quieter way: at 390px the **entire** visible text of the opened dock was `▶`, `♪`, `▾` plus a naked `80`, because the BPM label was `display:none` below 400px and the two sliders were unlabelled — one tempo, one volume, and she had to guess. Every control now sits in a 44px field with a caption riding in the dead vertical space each 44px control already has above its own content, so **the dock's height does not change and the reserve still clears it by 11px** at all 33 width × time-signature combinations. Visible text now: `▶ · BPM 80 · BEAT ●●●● · ▾ / TEMPO · METER 4/4 · VOLUME`. **The critic earned its keep three times.** It killed my first caption vocabulary with a measurement: `METRONOME` sat over the beat dots, and `elementFromPoint` at each caption's centre returned the control for all four real ones and the **bare field** for that one — the word naming the product invited a tap that did nothing, which is BL-074's bug one layer down. `BEAT` on the dots and `METER` on the select means each caption names its own control, and `METER 4/4` also stops visibly asserting that 6/8 has six beats. It found a **Level A WCAG 2.5.3 failure** — visible `BPM` against `aria-label="Tempo in beats per minute"`, which does not contain the string, so a voice-control user saying "click BPM" gets nothing — in a comment that enumerated exactly the two pairs that passed. And it measured what no gate can see: the running fill is **1.08:1 against the light-theme downbeat**, and the reduced-motion block deliberately removes the only other cue, so a light-theme reduced-motion user saw **nothing at all on beat 1** — the beat the CSS comment itself calls the one that must never be lost. Both indicators now grow on every beat via a `box-shadow` ring no reduced-motion rule can strip: lamp 14 → 17 → 20px, dots 10 → 13 → 18px, verified under `--force-prefers-reduced-motion`. Filed **BL-082** (the dock does not scale with text zoom: captions first collide at an **18px** root, not the 22px the comment claimed, and at 32px the caption crosses the dot row, the tempo caption meets the slider thumb, and the select's value hangs below its border box — the fix is rem, and it needs its own negative test), **BL-083** (nothing visible still says *metronome*, and a title needs a treatment that is not a control caption), **BL-084** (tab order through the dock contradicts its visual order at ≤720px). Also corrected `design/system.md` §2/§3/§7, which still documented the `▶`, the `♪` and "colour only" — a future agent following the spec would have restored exactly what this iteration removed. **BL-059, the seal, is now blocked-human** after four attempts: every closed rounded silhouette in this two-colour palette reads as a container, ten dead directions are recorded, and the two that remain are the owner's taste call. Gates: build 0 · 13/13 checks · beat-count byte-identical · `--gate` OK with no metric moved.
- 2026-08-08 · iter 58 · [content] **Lesson 1 now contains the diagram it always told you to point at** [BL-071] — the beginner persona's blocking finding, on the lesson the homepage's one big "Start here" button sends every new visitor to. It had zero images, taught kit anatomy in prose, and its own checklist said *"Can point to each piece on a diagram or photo and identify which line/space it sits on in drum notation."* Two panels now: a seating-view layout of all eight pieces, and — the one that earns the item — a five-line staff with each piece's **name sitting on the same line or in the same space as its notehead**, position spelled out in words. A beginner can read "Snare · 3rd space" and see it. Measured at a true 390px: zero overflow at 280/320/390/1280 and six more widths, labels 14.4–14.6px, no label collisions, both themes. Accessibility is a `<figure>` + `role="img"` plus a **visible** nine-item key as the long description, so it is resizable selectable prose rather than a hidden duplicate — which also let the old buggy enumeration be deleted. **Two things the build found on its own, both worth more than the diagram.** The lesson's prose was *wrong* about two notation positions — it claimed "ride and hi-hat (top line)" and "mid tom (next space down)" — and the Builder checked real VexFlow geometry instead of believing the paragraph. I re-verified: lines render at y 58/68/78/88/98 with a ledger line at y48, so the ride is the top line and the hi-hat sits *above* it. A diagram contradicting the paragraph beneath it would have been worse than none. It also **rejected its own first design** after screenshotting it and judging the noteheads an unreadable smudge at 390. Honest limit, which I agree with: panel 1 is a schematic of labelled shapes and does not show what a drum looks like — a photo is still better and stays blocked on the owner (BL-030). Filed **BL-081**: rendered staves emit literal `#444` and `#999999` that the renderer's currentColor sweep misses.
- 2026-08-08 · iter 57 · [ui] **Fourth attempt at the seal: nothing shipped, and that is the result** [BL-059] — a Designer produced **27 rasterised sketches**, all judged at true 16 and 28px rather than large, and four finished candidates. I read the contact sheet at the actual pixel grid and **rejected all four**: at 28px they read as shallow vessels with an implement in them — ashtray with a cigarette, wok with a spatula, loaf with a knife. The Designer called ashtray a "residual risk"; on the pixels it is the *primary* read. None beat what is shipped, so shipping one would have been motion without improvement. **The diagnosis is the yield.** The original failed as a *tall* cylinder reading as a barrel; these over-corrected into *shallow* dishes reading as pans. The misread migrated between vessel types instead of dying — barrel, honey pot, frying pan, ashtray, wok. Every closed rounded silhouette in this two-colour palette reads as a container. Four measured facts worth more than the candidates: **`--gold` is 2.3:1 against the light page ground**, so the current mark's *largest* element and its three rods are its lowest-contrast parts in the palette most visitors see; **gold against accent is 1.35:1 in dark**, so a gold head and an accent stick cannot be separated by colour at all; a striking angle only reads as a strike between **28° and 38°**; and ten dead directions are now recorded with what each actually reads as — stool, fish skeleton, wifi, fried egg, candle on a cake, beetle with antennae, hamburger, gear, coin, cancel button. The next attempt should stop drawing a drum in elevation: the untried directions are an *open* silhouette that is not a closed shape, and abandoning the instrument entirely — the name is drum.**town**, and a mark need not be the drum. Bookkeeping-only commit; no site change.
- 2026-08-08 · iter 56 · [loop] **Discovery pass — +2 items, and one false alarm I caught on myself.** First audit of page-level metadata across all 228 pages. Real finding: **three pairs of lessons ship identical `<title>` values** — "Basic Fills" on both `basic-fills` and `rock-basic-fills`, "Questlove Time-Feel" on both `funk-questlove` and `hiphop-questlove`, "The Six-Stroke Roll" on both `fusion-six-stroke-roll` and `six-stroke-roll-found`. In each pair the lessons genuinely differ (foundations vs genre-track treatment), so a reader with both open in tabs can't tell them apart and a bookmark is ambiguous. Filed **BL-079**; **BL-080** for the 404 sharing the homepage's meta description. **The false alarm is the more useful part:** the same sweep reported *228 pages with no canonical link* and *a sitemap with zero entries*. Both are artifacts of my local build — `site.js` reads `process.env.SITE_URL`, `netlify.toml` sets it, and the templates are gated on it. Rebuilt with `SITE_URL` set: 1 canonical per page, 227 sitemap locs. I nearly filed two phantom P1s off my own harness, so the check-before-filing is recorded in `meta.discoveryNotes` for whoever sweeps next. Snapshot: 852 exercises · 448 listening entries · 217 lessons · 228 pages · **13 gates** · listeningArtistNotDrummer 10 (from 103) · thinProse 33 (from 43) · noSubheadings 48 (from 58) · britishSpellings 0 · pxFontSizes 0 · headingOrderViolations 0 · svgsMissingAria 0 · silentHits 0 · integrity all 0. No parked items; one blocked-human (lesson photos). 29 ready, 50 done.
- 2026-08-08 · iter 55 · [ui] **The notation floor is density-aware, and the constants are now derived** [BL-040] — the flat 70% was a *percentage of natural width*, and natural width is itself capped at 1400 units for multi-bar phrases, so density got squeezed twice. Measured across the 432 staves that took a floor, it delivered between **13.6 and 116.7 px per notated event** — an 8.6× spread, with the dense end at ~5px of sticking ink. That is exactly the illegibility PKG-7 fixed for *single* bars and silently left in place on multi-bar ones; `jazz-modern-jazz#4` at 72 notes across 12 bars was the worst. Replaced with `max(natural × 0.70, events × 28)`. Every constant is derived and documented in `.eleventy.js` instead of asserted: 28px/event follows from VexFlow writing annotations in **points**, so 14pt sticking is 18.67 user units and rendered size is `18.67 × (pxPerEvent / 52)` — PKG-7 measured 10px as legible at a true 390, and 10 = 18.67 × (28/52). 52 is the renderer's own per-note allowance; 620 is unchanged, being the width below which a stave already fits a phone. **Measured after:** 461 staves carry a floor, the ratio now spans 0.70–1.44, and **eight staves floor *above* natural size — they scale up and pan rather than shrink**. On `rock-dynamics` at a true 390px the dense frames render a 1400-unit viewBox at 1792px, scale **1.28 where it used to be 0.70**, so notes are 1.83× larger. Page overflow stays 0 at 280/320/360/390/480/720/1280 across four page types, because the frames scroll internally. The widest floor is 2016px — about 5.6 screens of panning at 320px, which is a lot, and is the trade §6.5 states outright: honest panning beats illegible shrinking, and nothing is dropped at any width.
- 2026-08-08 · iter 54 · [quality] **A gate for tips that contradict their own notation** [BL-070 done] — last tick I shipped the screener as a hand-run tool because it cried wolf 6 times in 13. It is now the 13th check: **51 claims verified, and negative-tested** by reintroducing the exact metal-headbang bug, which it catches with the precise diagnosis. Getting to zero false positives took four passes and **two of the wrong turns were mine**. I invented a note-count grid for shuffles and made that case *worse* — the backbeat moved from 2.5 to 3 — when the corpus already declares those bars with a `tuplets` array and `audit-lessons.js` has been scaling by it correctly all along against a committed zero baseline. I had also read `dot` as a "." in the duration string when it is a separate boolean. Reading the proven implementation first would have saved both. Four false-positive classes are now handled explicitly, including one I had never seen: `independence-singing` Ex 1 writes the **sung** pulse on the snare line, because that is the only place to put a vocalised quarter on a drum stave, while its tip describes a played part it says outright is *"not notated above"*. Checking one against the other was a category error, not a defect. Five exercises stay on a documented UNPARSEABLE list with a stated reason each — one of them is BL-078 unmarked ghosts surfacing as a second symptom. The failure message tells the next reader to decide **which side is wrong** before editing, because chunk 1 found two specs that were the bug.
- 2026-08-08 · iter 53 · [ui] **Shift+Tab no longer parks the focused row behind the header** [BL-072] — the keyboard persona's one blocking finding, and the only place on the site where they lost their cursor. `scroll-margin-top` existed only on `[id]`, which covers anchors and *forward* tabbing; going backwards the browser scrolls the newly focused element to the **top** edge, which is where the header is. **My first negative test passed when it should have failed** — I focused elements in reverse DOM order and got 0 buried both with and without the rule. A fix whose removal changes nothing is not a verified fix; my harness never exercised the path, because sequential focus from the bottom keeps everything near the current scroll position. Rebuilt to replicate the persona's actual walk (homepage, scroll to y=3000, step backward letting the browser scroll each row in) and it reproduced their numbers: **10 of 26 stops behind the header without the rule, 0 with it** — including "04 Two-Bar Phrasing" at top=29 and "03 Backbeat Color" at top=0, the same rows they named. Also checked that anchors are not double-offset, since two rules now set the property: #foundations/#tracks/#mastery still land 10.6–11.3px below the header. The persona had proposed `html { scroll-padding-top }`, which fixes focus but *would* have stacked with the existing rule and pushed every anchor jump twice as far. Dropped BL-058 to P2: it was filed around iter 30 and last week's three fresh personas never raised nav vocabulary.
- 2026-08-08 · iter 52 · [content] **Two exercises were playing the wrong backbeat** [BL-070 chunk 1/2] — the returning persona hit a tip contradicting its notation on the exact lesson the site sent them to. Screened all 852 exercises: 58 tips make a checkable "snare on X and Y" claim, 13 didn't match. Then adjudicated each by asking **which side is wrong**, with refuters told to be hardest on any "the notation is wrong" verdict — because changing a spec changes what the site *plays*, and fixing a tip to agree with a broken spec would launder the bug into the teaching. Result: **6 false positives (my own script), 4 tips wrong, 2 notation bugs.** `metal-headbang` Ex 2 played its backbeat on the *&* of 2 and 4 while every sibling exercise, the lesson body and its own title said otherwise. `rock-hybrid-grooves` Ex 2 the same — and the refuter closed the swing-meter escape hatch by pinning the corpus's own vocabulary (exercises whose *titles* name beats put their notes at t=1.0/3.0), swept all 27 exercises sharing that ride shape to show this was the only outlier, then caught the identical buggy block **copied into Ex 4's bar 2**, which had to move in the same commit or the lesson would contradict itself. Snare indices asserted after the edit and the corrected stave read on screen. The screening tool ships as `tools/audit-tip-claims.js` but **deliberately not as a gate**: 6 false positives in 13, in two documented classes (ghosts are also `c/5`; compound meters counted in the wrong beat unit), and a gate that cries wolf half the time trains people to ignore it. Filed **BL-078**: ghost notes chorded with a hi-hat note carry sticking "r" in three lessons — physically impossible when the right hand is on the hat. The letter has drifted from meaning a *hand* to meaning a *ghost*, and the adjudicator was right to refuse to patch one lesson in isolation.
- 2026-08-08 · iter 51 · [ui] **PKG-10 release gate — the communal-rebrand epic closes** [BL-057, BL-047] — ten packages, iterations 25–51. Sweep: `style=` now 0 across all 11 templates, no `@import`, nothing under the 11px floor, DARK-TOKENS byte-identical by sha, fonts 110.5 KiB of a 120 KB budget. Keyboard walk: **596 rendered stops, 0 without a focus ring**. Reduced motion verified with the real flag. **Two corrections to my own work.** My orphaned-CSS list had *four false positives* — I derived fourteen from a 40-page sample, and `.level-block__tag`, `.exercise-desc`, `.exercise-lesson-link` and `.notation-error` are all live or deliberate; the `.tips`/`.tag` "hits" were prose ("the tips should rise"). And **BL-044 was not closed**: I marked it done at iter 40 because the chip grid carrying the orphaned separators was deleted, but the breadcrumbs had already reinherited it at 280–320px. Closing an item because its host component vanished closes the symptom, not the mechanism. **The persona review says the rebrand landed** — the beginner: *"within about four seconds I knew this was a real drum school and not a funnel"*; the returning player: prose *"written by somebody who actually plays"*, notation *"properly engraved, not a screenshot of tab"*. **But BL-059 failed its acceptance and I got it wrong.** All three read the mark as a vessel — *"a small wooden barrel, or maybe a honey pot"*, *"a little barrel, or a tin, or one of those wooden kegs"*, *"honestly a jar of jam"* — and all three saw a snare drum only after magnifying it. In iter 49 I claimed the bottom hoop made it read as a drum, based on my own look at a magnified render. That is precisely the mistake the persona check exists to catch. Filed **BL-071–077**, three blocking: lesson 1 asks you to point at a diagram it doesn't contain; Shift+Tab parks focus behind the sticky header; progress marks exist on the homepage and nowhere else.
- 2026-08-06 · iter 50 · [content] **The ten thinnest lessons, expanded** [BL-022 chunk 2/6] — `fusion-odd-meters` was **64 words** for an entire lesson; it's now 702. Ten lessons went from 64–129 words to 548–797, each with two descriptive h2 seams. Lessons with two paragraphs or fewer: **44 → 34**. The binding rule was that every technical claim had to be grounded in that lesson's *own* exercises, because a wrong instruction on a drum site doesn't just misinform — it gets practised and the habit sticks. Writers returned each claim *with* its grounding; a reviewer checked every statement against the real specs and rejected a lot of plausible prose: a kick "8th-note grid" that doesn't exist, "both feet on pedals in every exercise" when Ex 1 is explicitly single-foot, "same tempo" across exercises 20bpm apart. The best catch was an instruction to "set the metronome to quarter notes" — refuted by opening `base.njk` and finding the site metronome **has no subdivision control**. A UI-truth error caught by checking the UI. **The writers also found something bigger than thin prose, now BL-070 (P1):** five lessons carry tips or graduation criteria that contradict their own notation. `metal-headbang` Ex 2's tip says "snare still on 2 and 4" while the array puts it on the *&* of 2 and *&* of 4. `fusion-polyrhythms` claims two voices "only line up on beat 1" when 6:4 reduces to 3:2 and they coincide every two beats — and that error sits in the prose, the tip, *and* a graduation criterion, so fixing bodyHtml left two of three wrong. `rock-quarter-hat` offered Charlie Watts as its model when his signature move is the opposite of what's notated.
- 2026-08-06 · iter 49 · [ui] **PKG-9 notice-board footer + a seal that reads as a drum** [BL-056, closes BL-009] — four columns, the motto "The door's open. Bring sticks.", and a counts line recomputed from the data files to check the page isn't lying (217 · 852 · 40). 52 measured rows across 13 widths and both themes: zero overflow anywhere, 45px minimum link target at 390. **The seal is redesigned.** Maya's read back in iter 30 was "a record or a vinyl label first… it isn't shouting drums at me" — and she was right, two rings and four dots *is* a vinyl label on a site called drum.town. It's now a snare drum in elevation. The Builder sketched six candidates and rasterised each at **16px** rather than judging them large, which is the only test that matters for a favicon: a lugged hoop read as a gear, a three-quarter view as a coin, crossed sticks as a cancel button. I checked its shipped version myself and the protruding side lugs still read as a *bucket with handles* — the exact risk it had flagged on an earlier candidate and thought it had designed out. Adding a bottom counter-hoop fixes it: drums are hooped top and bottom, buckets aren't, and the symmetry survives 16px. **BL-059 stays open** — its acceptance wants a persona to name the mark unprompted, and I can prove it's no longer a record but not what someone will call it. **A bug no gate here could have caught:** the first `seal.svg` had a doubled hyphen inside an XML comment, which is a fatal parse error, so the favicon rendered as *nothing* — invisible in the tab, silent in the console, and invisible to all 12 checks and the metric gate, none of which parse SVG assets. Filed **BL-069** from the same Builder working outside its scope: at 200% text the homepage scrolls sideways below 480px, and it's `.ledger__title` taking min-content as a flex item, not the footer.
- 2026-08-06 · iter 48 · [content] **Attribution campaign closes: 103 → 10** [BL-063 done, BL-068 done] — five chunks, every change researched from a fetched page and then refuted by an independent skeptic. **The ten that remain are not unfinished work.** Each is a documented hold where naming anyone would mean inventing a fact: *Freudian* carries no drums credit for anyone at all; *Black Messiah* has three drummers and no source naming a track where Chris Dave is the kit; *Magdalene* has twelve people holding a drums credit and not one drum-*kit* credit; *Catch Thirtythree* and *Altered State* were programmed, not played. The best of them is Bo Diddley's 1955 Checker 814, where the Library of Congress registry essay names the entire session and then declines on the one seat that matters — "and a drummer, either Frank Kirkland or Clifton James." Sources split down the middle, and Bo Diddley's own quote about who "did the original Bo Diddley beat" is a claim about who *invented* it, not who sat behind the kit that day. The historical record doesn't know, and printing either name would be a coin flip dressed as a credit. **BL-068 closed, and it was worse than filed**: both track titles in that lesson's body were *invented* — not on the 2018 album's fifteen tracks, not on the 2013 mixtape's twenty-one, not misspellings of anything. The refuter re-derived that negative itself rather than trusting the researcher, then **rejected the proposed replacement sentence** for upgrading a source's "subtly displaced playing style" into a specific snare placement, inventing a "90s-leaning" framing lifted circularly from the site's own listening card, and duplicating an entry that already names all three tracks. A third fabricated search citation was caught and refused this round — a personnel list for a Bobby Byrd track whose cited page has no personnel section at all.
- 2026-08-06 · iter 47 · [ui] **PKG-8 genre, rudiments, metronome, 404** [BL-055] — the last four old-shaped pages join the system, so the rebrand is visually complete. Genre pages get the real `.breadcrumb` (**closes BL-032**) and their card lists become ledger rows *reused* from PKG-5, so every list on the site now speaks one language. Rudiments gains a family index — `Rolls 15 · Diddles 4 · Flams 11 · Drags 10`, counts the Builder derived itself so the chips pay off the "40 Essential" headline. Verified across all 7 tracks: 141 lessons resolve, every duration matches `lessonContent`, every row href lands on a page whose h1 equals the row label. **The Builder caught a bug only driving the page could find** — its first metronome script ran at parse time, but `#site-metronome` is an aside printed *after* `</main>`, so the lookup was null, the script bailed silently, and the button shipped permanently hidden. Its words: "Had I only taken screenshots, this would have shipped." **Two of the panel's three findings were my own mess:** both lenses independently found `_site/__f8.html`, a screenshot harness I wrote and forgot to delete, which was failing the gate at that moment — and both warned, correctly, never to clear it with `--write-baseline`, which would have baked a page with no `<main>` into the committed baseline forever. Protocol amended: harnesses go under `_site/dev/`, the one directory the audit walk skips. The third finding was real and mine to fix: `dc_metro_collapsed` persists, so on a *second* visit the dock is already open while the button still said "Open the metronome" and pressing it silently moved focus. The label now tracks the dock's actual state through a MutationObserver, since the pill, the chevron and Escape can all toggle it from under the page.
- 2026-08-06 · iter 46 · [loop] **Discovery pass — +3 items.** Link integrity is clean: **11,297 anchors across 228 pages, zero broken internal links, zero links without accessible text.** Everything the epic has rebuilt still connects. Three new items from the sweeps. **BL-068 (P1)**: `hiphop-chris-dave` carries a section *heading* — "Drumhedz / Tabernacle" — and prose built around two tracks that are not on the album's fifteen-track listing; a student following it finds nothing. Found by a BL-063 researcher working outside its own scope, and reported rather than ignored. **BL-067**: `p.callout-warn` appears four times in lesson content and zero times in the stylesheet, so four warnings render as ordinary paragraphs. **BL-066**: fourteen CSS rule-sets are orphaned — `.tracks-grid` and `.curriculum-section*` were the homepage chip grid PKG-5 removed, `.lesson-meta` was the title block PKG-6 replaced — plus `curriculum.generalistPath`, data rendered by no template. Snapshot: 852 exercises · 448 listening entries · 217 lessons · **listeningArtistNotDrummer 18** (from 103) · duplicateListeningPicks 8 · britishSpellings 0 · pxFontSizes 0 · outlineNone 0 · headingOrderViolations 0 · svgsMissingAria 0 · pagesMissingMain 0 · silentHits 0 · multiBarSpecsUnsupported 0 · focusDistinctValues 23 · integrity all 0. No parked items; one blocked-human (lesson photos). 25 ready, epic in build with PKG-8/9/10 left.
- 2026-08-06 · iter 45 · [content] **Drummer attributions, chunk 4** [BL-063] — **29 → 18**, and this batch asked a different question. Mostly hip-hop, R&B and Latin, where the honest answer is often that *no drummer exists*, so every entry carried a `kind` — played / programmed / mixed / ensemble / unknown — and the refuters had to check it as hard as the name. Crediting a human for a machine part teaches a student something false about how a record was made. Things only that field surfaced: Slum Village's *Fantastic, Vol. 2* is J Dilla hand-programmed on MPC pads with the quantize off, not a kit; the Pedrito Martinez Group has no drum set at all; Wilfrido Vargas's record credits a *tambora* player rather than a kit, so the note now tells the student which figure their tom part stands in for. Two held because no single name honestly authors the drums — *Magdalene* has twelve people holding a drums credit across nine tracks and not one drum-*kit* credit anywhere, and *Freudian* carries no drums credit for anyone. **On *Magdalene* I shipped the corrected note without the name.** The old text read "hybrid live-electronic — kit and producer-side blurred", asserting a kit that is not on the record, on a lesson whose body uses that exact phrase to mean a drummer playing alongside triggers. Removing a false claim doesn't require the attribution to be settled first, and the entry still counts as a violation, so the metric isn't being gamed. Also caught by hand: three refuters said to keep a bare work title because "the artist parenthetical already carries the leader" — but the parenthetical is exactly what this pass removes, so that would have dropped the credited act. `duplicateListeningPicks` 9 → 8.
- 2026-08-06 · iter 44 · [ui] **PKG-7 the reading room** [BL-054] — prose set, exercise card, and the mobile notation strategy. Sticking annotations go 10 → 14 and the wide-stave scroll floor 55% → 70%, so **sticking letters at true 390px measure 10px of ink, up from 5px** — measured, not computed. Panning frames get a right-edge fade and a visible scrollbar; nothing is hidden or truncated at any width. **My own premise was wrong and the Builder disproved it.** Iteration 42 recorded that the 200%-text overflow came from fixed-width notation staves and filed it here; it does not. The staves sit inside `overflow-x: auto`, so their width never reaches the document scroll area — widening them from 418px to 532px moved page overflow by exactly zero pixels. I re-measured independently and the Builder was right. The real causes were the site header, whose collapse was keyed to *viewport pixels* so raised text never triggered it, and one unbreakable record title in a listening row. The header's breakpoints are now `em` (53.75em and 22.5em — byte-identical to 860px and 360px at a default root, verified at 858/859/860/861/862), because an em media query resolves against the browser's own font size and so collapses the nav when text is enlarged, which the px query could never do. Honest limit: a harness that sets `html{font-size}` moves rem lengths but *not* media-query ems, so the raised-text path is reasoned from the spec and confirmed only at the equivalence points. Filed **BL-065** from the Builder's own boundary-drawing: eight dense multi-bar staves already overflow their frames and its retry loop deliberately excludes them rather than letting them ride along unnoticed.
- 2026-08-06 · iter 43 · [content] **Drummer attributions, chunk 3** [BL-063] — the batch earlier chunks kept deferring. **52 → 29.** The refuters earned their keep three times over. *Wave*, held in chunk 1 because three drummers are credited flat, turned out to have a source saying plainly that only Claudio Slon played the kit and the other two played hand percussion — and the refuter then **rejected its own researcher note**, which claimed the kit steps forward on "Captain Bacardi" when the source says the percussionists do. `metal-gravity-rolls` cited *Echoes of Decimation* for John Longstreth, who was out of the band when it was recorded; the album was changed rather than the name, with the refuter insisting both fields ship together or neither, since writing the name alone would have shipped the original error. And *Chega de Saudade*, held in chunk 1 because its note was entirely about Gilberto's guitar, now points at Milton Banana's brush-and-rim kit. Held: *Black Messiah*, where Chris Dave's contribution is real but unlocatable — Questlove is on ten of twelve cuts and no source names a track where Dave is the kit, so it cannot work as a listening assignment. `content.duplicateListeningPicks` 8 → 9 again with no new citation added: correcting one artist merged it with three entries already spelled that way.
- 2026-08-06 · iter 42 · [ui] **PKG-6 lesson title block + map disclosure** [BL-053] — **closes BL-037**: at a true 390px the lesson h1 now sits at top 174, inside the first viewport, where the entire curriculum sidebar used to be. The sidebar is a 48px closed-by-default disclosure bar below 960px and a pure-CSS sticky column above it (verified with every script tag stripped). The `.eyebrow` line is deleted from all 217 lesson pages and the section name appears twice instead of four times. **Four defects survived the all-gates-green report.** The Builder caught one itself and said so rather than hiding it: U+23F1 STOPWATCH has *emoji* presentation by default, so the duration chip looked like a monochrome glyph on this Windows machine but would have shipped as the site's only colour emoji on iOS and most Androids — no screenshot taken here could have caught that. The panel found the rest: the disclosure bar's kicker never shrank, so raising the browser text size painted the chapter name through the caret and off the edge, giving 114px of horizontal scroll at the 200% WCAG 1.4.4 requires. And the prereq pills measured **1.00 contrast against the surrounding prose** and 1.14 against the *static* chips sixty pixels above — two rows of identical pills, only one clickable. **The panel's own verifier had refuted that one; I measured it and the original reviewer was right.** Residual and explicitly not fixed here: at 200% text the notation SVGs still overflow, because stave widths are written in px — recorded against PKG-7, which owns mobile notation.
- 2026-08-06 · iter 41 · [content] **Drummer attributions, chunk 2** [BL-063] — ten more lessons, 32 entries, every one holding a band name where the drummer belongs. `metal-extreme` credited Nile, Behemoth, Cannibal Corpse and Napalm Death; `jazz-modal-comping` credited three records and named no drummer on any of them. 30 cleared by a research-then-refute pass, 3 held. **content.listeningArtistNotDrummer 81 → 52**, distinct artist strings 250 → 238. This round the refuters supplied replacement note text as well, which closed chunk 1's weak spot: a note that opens with the drummer's name prints it twice once the artist field holds that name, and several notes turned out to be bare mood descriptions ("death-metal weight with surgical timing") that said nothing at all once the name came off the front — those were rewritten to name something you can actually hear. Held for cause: *Human / Individual Thought Patterns* cites two albums with different drummers; *Night Visions* credits its drummer "except 3, 6 &…", so it is not a single-drummer record.
- 2026-08-06 · iter 40 · [ui] **PKG-5 ledger homepage** [BL-052] — the two-state hero swap and the chapter chip grid are gone. One thesis headline ("The whole town practices to the same clock."), live counts, and a ghost "Continue — *next lesson*" that appears only for returning visitors; below it, 19 chapter ledgers of numbered rows with real minutes, dotted leaders, and ✓ done / ● here / ○ marks that are announced as well as painted. Closes **BL-044** and the hero half of **BL-045**. **Five defects survived the Builder's all-gates-green report.** Three I caught by measuring: the "you are here" row rendered its number green while everything else on it was accent (that row carries *both* state classes); the mark kept a fixed 64px column when stacked, stranding a bare ○ in open space; and the ledger filled 1128px against a 720px hero, stretching the dotted leader to 735px — further than an eye tracks from a title to its minutes. Two came from the adversarial panel: completion was gated on the *position* of your last lesson rather than on coverage, so opening the final lesson — one click from the homepage's own last row — told a first-time visitor "You've finished every ready lesson" while 216 rows still showed ○ and the button above still said "Start here"; and the chapter head stacks earlier than the rows do, orphaning "N LESSONS" onto its own line between 561–631px. Both fixed and verified in a browser. The panel refuted 15 of 17 findings, including a claimed bfcache staleness bug.
- 2026-08-06 · iter 39 · [content] **The listening sections were crediting bands instead of drummers** [BL-063] — the style guide has always said "`artist` is the drummer, never the band — that is the whole point of the section", and `check-listening.js` claims to enforce the field contract, but it only catches a leading "The". **103 of 448 entries walked past it.** The lesson *about Elvin Jones* credited "John Coltrane Quartet" and "Elvin Jones Trio" and never named him; the Tony Williams lesson credited "Miles Davis Quintet"; `latin-bossa-nova` credited Antônio Carlos Jobim, a composer and pianist, for a record he did not drum on. Fixed 22 entries across 9 lessons, each attribution researched from fetched sources and then handed to an independent skeptic told to refute it (24 of 32 cleared, 8 held). New ratcheting metric `content.listeningArtistNotDrummer`: **103 → 81**. Two data-quality fixes also removed *masking*: unifying four spellings of Questlove, and merging band-keys into drummer-keys, exposed four picks that were already over the 3-lesson diversity cap but had been counted as separate works — so `duplicateListeningPicks` rose 7 → 8 without a single new citation being added, and the baseline was raised for that reason and stated plainly rather than quietly. Held, with reasons: *Wave*, *Catch Thirtythree* and *Altered State* (programmed or multi-drummer, so no single name is true), *Chega de Saudade* (right drummer, but the note is entirely about Gilberto's guitar — fixing the artist alone would point a student at a drummer and then tell them to listen to a guitar).
- 2026-08-06 · iter 38 · [ui] **PKG-4 transport dock + clock pill** [BL-051] — the corner metronome card is gone; a tempo pill in the header opens a full-width dock along the bottom, and the body reserves its height. Closes **BL-036** structurally and narrows BL-043 to one item. The Builder reported success and all gates green; **four real defects survived that report**, none of them visible in a screenshot. Two I found by measuring the reserve instead of looking at it: `--transport-h` undershot the real dock height by 8px (the zero-height flex break element is zero-*height* but not zero-*line*, so the row pays two row-gaps), and the beat-dot row's `flex-basis: auto` let 7/8 on a 320px phone wrap the dock to 181px against a 140px reserve. Two more came from the adversarial panel: the pill gave all 228 pages a sideways scroll below 333px (WCAG 1.4.10 reflow fails at the normative 320px, which is also 400% zoom on a desktop) and put the hamburger off-screen at 280px; and the sticky curriculum sidebar — sized to the viewport, so body padding slides past it — buried its own last entry under the dock at every desktop width, where clicking it typed into the BPM field. Also fixed: dock stealing mobile-nav taps in landscape, reduced-motion losing the cascade so the pulse never stopped, the pill advertising `aria-expanded` while only ever expanding, and an accessible name that would have read "80beats per minute" and never said whether the click was running. New gate `check-transport-reserve.js` drives a real browser at 12 widths × 3 time signatures and asserts all three invariants; **negative-tested four times, once per bug class**.
- 2026-08-06 · iter 37b · [content] round 2 — Copeland and Jo Jones passed on facts but had their notes **rewritten** before shipping (Copeland's contrasted two different cymbals on a lesson about striking one cymbal in different places; Jo Jones's promised a "chick" a 240bpm 1938 shellac can't deliver). Ben Riley rejected outright: its note called the foot inaudible, on a lesson about hearing the foot. Over-cap works 7→5. Three verified picks shipped; six slots deliberately left citing an over-used record rather than filled with a guess.
- 2026-08-06 · iter 37 · [content] listening diversity round 1 [BL-029] — an 18-agent propose-then-fact-check pipeline produced **1 accepted, 8 rejected**. Every rejection was the same failure: right drummer, real record, invented technique. One verifier caught search engines *fabricating citations* and threw them out. Shipped: Carlton Barrett / Bob Marley — Exodus for three-limb-patterns (also the site's first reggae pick). Aja 6→5. Five verifiers explicitly refused to name a substitute rather than guess; three offered sourced swaps, now in a second adversarial round.
- 2026-08-05 · iter 36 · [content] three exercises stopped promising meter transitions they never notate [BL-061] — retitled/reworded to point at the companion exercise that *does* cover the other meter. The fourth was left alone: "5/4 with implied 7/8 grouping" is exactly what's on the stave. Discovered the acceptance's preferred fix is impossible today — the renderer takes one time signature per spec — so **BL-062** is filed for per-bar meters, which is what these lessons actually deserve.
- 2026-08-05 · iter 35 · [content] listening entries [BL-046 part] — rejected the "four inconsistent formats" premise after reading all 448: the variation encodes who the record is credited to, and flattening it would have invented bands. Wrote the conditional rule into the style guide instead. Fixed what was actually wrong: 4 entries credited a band where the drummer belonged (their own notes named Zigaboo Modeliste, Chris Pennie, Hal Blaine) and 85 notes had no terminating period. 11th gate.
- 2026-08-05 · iter 34 · [content] exercise titles now follow one scheme [BL-026] — 24 normalised to "N — Title" (orphan "1A"s with no sibling, numbers that didn't match position, one lesson starting at "0", double em dashes). Kept deliberately: 7 real variation pairs and 6 named-sequence titles whose prose cites them. New gate `check-exercise-titles.js` catches duplicate/gapped numbering — which is exactly what my own edit briefly created when three titles with escaped quotes failed to match.
- 2026-08-05 · iter 33 · [content] **focus chip is a controlled vocabulary at last** [BL-024] — the label on every one of the 217 lesson pages went from 144 free-text values (casing collisions, order collisions, one-offs like "Speculative / Hybrid / Frontier") to 23 real categories; locked by `check-focus-vocab.js`. Reviewing the mapping before applying caught a rule-order bug that would have emptied the Counting category.
- 2026-08-05 · iter 32 · [content] clipping fixed in the other direction too (7 feet-tuplet brackets cut off below the stave) and locked by `check-notation-frame.js` over all 932 staves; 8 exercise metas stopped advertising tempo ranges the Play button can't honor (moved to the tip), locked by `check-exercise-meta.js`. Both gates negative-tested. Four exercises whose meta narrates more meters than the notation shows are filed as BL-061 rather than mangled.
- 2026-08-05 · iter 31 · **TRIPLET BRACKETS WERE BEING CLIPPED OFF THE STAVE** [BL-060] — every shuffle lesson drew its "3" brackets at negative y, outside a viewBox pinned to origin 0, so the notation never showed that it was a triplet at all. Renderer now grows the viewBox to the drawn content (17 exercises, worst −45px). Also fixed the header breaking between 720–860px ("Other Topics" wrapped to two lines).
- 2026-08-05 · iter 30b · **83 WRONG BEAMS FIXED ACROSS THE CURRICULUM** [Dylan spotted one; it was a whole class] — "Compare Straight to Triplet" beamed 4+2 instead of 3+3. Root cause: `beamGroups` are REAL played time, so `[3,8]` inside triplets means 1.5 beats, not 3 notes. Fixed 34 exercises (every shuffle lesson, all Elvin Jones, Purdie half-time, six-stroke, Moeller, metric modulation) + a renderer bug where rests were stripped before beaming, making the grouping clock skip their duration. New gate `check-beam-groups.js` (negative-tested) makes this class un-reintroducible. audit-lessons never saw any of it — the durations summed correctly; only the picture lied.
- 2026-08-05 · iter 30 · **DARK MODE + REAL MOBILE NAV LIVE** [BL-049 + BL-050] — hourly cadence, two packages per tick. Header rebuilt (seal + drum.town wordmark, Metronome link finally reachable, hamburger disclosure at ≤720px), full dark theme with a persisting sun/moon toggle and no flash. Panel caught what I couldn't: Sam (a11y) found two BLOCKING keyboard bugs — opening the menu sent focus away from it, and following a link dumped focus to `<body>` — both fixed and now guarded by a new gate (`check-nav-a11y.js`, negative-tested). Maya (beginner) found the dark menu rows blurred into "one grey block" (fixed) and that no nav word says "Lessons" (filed BL-058). Also: dark notation ground softened from #fffdf7 to #f0e9db ("a flashbang at night", still 13.1:1).
- 2026-08-05 · iter 29 · **PKG-1 THE REPAINT IS LIVE** [BL-048] — drum.town now wears The Square: plaster/brick/park-green palette, Fraunces + Public Sans + IBM Plex Mono, new scale and elevation. Verified after the fact (11/11 contrast pairs, 0 structural changes, 0 selectors lost, screenshots read at both widths) because the Builder self-committed against instructions — protocol hardened so subagents can't touch git. NOTE: adversarial reviewer was cut off by the org monthly spend limit; orchestrator completed the review.
- 2026-08-05 · iter 28 · BL-039 [quality] acoustic kit stops faking cymbals — missing voices use honestly-synthesized recipes (no more one-sample-three-speeds); electronic ride gains its missing ping fundamental
- 2026-08-05 · iter 27 · BL-038 [ui] playhead follow now yields to the user — scroll-event ownership model (reviewer rejected my first timer-based fix as DEGRADED; shipped its better architecture: follow resumes when the playhead re-enters your window, works for scrollbar drags in every browser). Loop heartbeat switched to background-sleeper notifications after in-session timers failed twice.
- 2026-08-05 · iter 26 · SPEC COMPLETE — design/system.md (16 slots kept + 8 new, dual themes computed AA+, 113KB measured font plan) + design/packages.md (10 build packages, each one tick, site coherent after every deploy). Backlog loaded (BL-048..057); epic phase → build; ui cadence doubled. The rebrand starts shipping to drum.town next ticks.
- 2026-08-05 · iter 25b · DIRECTION DECIDED — "The Square" (A) wins 4-judge panel + orchestrator, amended with B's transport-dock metronome and C's ledger chapters; B unanimously last (stage ≠ town). All 42 contrast claims verified honest; every board dodged mobile nav + mobile notation legibility — now mandatory spec contract. See design/DECISION.md. DYLAN: veto window open — say the word to override.
- 2026-08-05 · iter 25 · PIVOT [Dylan] — communal-rebrand epic opened: full modern rebrand around the drum.town town-square identity (design language only, no backend). Loop protocol v2: Designer → Builder → adversarial Reviewer → persona panel, visual verification on every output tick. Discovery: pushes to main already deploy to drum.town (Netlify git integration) — all prior fixes are live; netlify.toml now pins SITE_URL so production regains its sitemap; GitHub Actions CI added (build + audit gate on every push)
- 2026-08-04 · iter 24 · BL-035 [ui] resolved as measurement artifact — Windows headless Edge floors at ~492px and crops, so all "mobile clipping" was a cropped healthy layout; true-390 iframe harness committed (tools/mobile-viewport-harness.html), protocol updated; the mobile site renders cleanly (BL-037 sidebar + BL-044 dashes confirmed real)
- 2026-08-04 · iter 23b · adversarial critique integrated — 4 factual listening errors fixed same-tick (50 Ways sticking claim, AOBTD kick claim, Tom Sawyer doubles claim, Green Onions swapped for Billie Jean), 3 style-guide violations in my own prose fixed (incl. the paradiddle banned opener), 9 new items filed (BL-038..046: playhead scroll-fight P1, audio voice honesty, density-aware notation floor, heading-audit sampling gap, and more); 16 of 31 new listening picks verified correct
- 2026-08-04 · iter 23 · loop upgrade [Dylan's feedback] — visual verification (headless-Edge screenshots, read + judged) and an adversarial critic subagent are now protocol steps 6b/6c; first visual pass found 3 real issues no gate saw: mobile horizontal clipping (BL-035, P1), metronome overlapping content at 1280px (BL-036), sidebar-above-lesson on mobile (BL-037)
- 2026-08-04 · iter 22 · BL-017 [ui] prefers-reduced-motion respected — animations collapse, metronome dots stop pulsing (color still marks the beat); css.missingReducedMotion 1→0
- 2026-08-04 · iter 21 · BL-007 [quality] rudiments page render cost fixed via content-visibility — off-screen cards skip layout/paint; scrollbar stays stable
- 2026-08-04 · iter 20 · discovery #2 — link scan clean, no new issues, nothing parked in 19 ticks; all remaining metrics map to ready backlog items
- 2026-08-04 · iter 19 · BL-023 chunk 1/6 [content] 31 listening picks added across 13 lessons (Foundations + track intros) — lessonsMissingListening 77→64
- 2026-08-04 · iter 18 · BL-016 [ui] dark-mode groundwork — notation ink routed through currentColor + tokens; the white/black island is no longer hardcoded
- 2026-08-04 · iter 17 · BL-006 [quality] legacy audit output cleaned — float noise rounded, clave Total added, baseline regenerated
- 2026-08-04 · iter 16 · BL-022 chunk 1/6 [content] 10 thin lessons expanded with practice-method + failure-mode sections — lessonsThinProse 53→43, lessonsNoSubheadings 68→58
- 2026-08-04 · iter 15 · BL-015 [ui] all font sizes in rem — user font-size preferences finally respected; css.pxFontSizes 79→0 (pixel-identical at default settings)
- 2026-08-04 · iter 14 · BL-005 [quality] npm run clean is cross-platform (node fs.rmSync) — verified on PowerShell, idempotent
- 2026-08-04 · iter 13 · BL-021 [content] gear-basics + tuning-basics get real exercises (pad strokes; strike-and-listen tuning drills) — lessonsZeroExercises 2→0; the homepage's "every lesson is playable" promise is finally true
- 2026-08-04 · iter 12 · BL-014 [ui] notation fits phones — single-bar staves size to content (427/888 no longer force horizontal pan), playhead auto-follows scrolling staves
- 2026-08-04 · iter 11 · BL-004 [quality] sitemap is spec-valid — emitted only when SITE_URL is set (absolute URLs), omitted otherwise; dom.relativeSitemapLocs 227→0
- 2026-08-04 · iter 10 · discovery — link scan clean (11,749 refs), no TODO/console.log, +3 polish items (BL-032..034); 12 of 26 gated metrics now at zero
- 2026-08-04 · iter 9 · BL-020 [content] style guide authored (docs/content-style-guide.md) — 24-value focus vocabulary proposed, voice/structure/exercise/listening conventions set; unlocks all rewrite chunks
- 2026-08-04 · iter 8 · BL-013 [ui] docked metronome is now collapsible — starts collapsed on phones (was covering ~45% of the viewport), choice persisted
- 2026-08-04 · iter 7 · BL-003 [quality] dead-code sweep — never-rendering preview card removed end-to-end, deprecated lesson.njk deleted, "ready" badge noise gone, hygiene.deadFeatureRefs 4→1 (generalistPath kept as noted data)
- 2026-08-04 · iter 6 · BL-019 [content] American spelling normalized — 119 British forms replaced across all 217 lessons, content.britishSpellings 119→0
- 2026-08-04 · iter 5 · BL-012 [ui] keyboard focus restored — global :focus-visible ring, all 4 outline:none suppressions removed (sliders had no indicator at all)
- 2026-08-04 · iter 4 · BL-002 [quality] multi-bar playback fixed via shared PatternMath — 153 exercises no longer overlap themselves; dotted-note timing (83 notes) fixed as a bonus; playhead syncs
- 2026-08-04 · iter 3 · BL-018 [content] paradiddle factual fix — "eight 16ths per bar" corrected to sixteen (four groups), matching the notation
- 2026-08-04 · iter 2 · BL-010+BL-011 [ui] main landmark, skip link, exercise h3s, all 888 SVGs labeled — dom.pagesMissingMain 228→0, dom.svgsMissingAria 888→0
- 2026-08-04 · iter 1 · BL-001 [quality] all 11 drum keys now sound — player.silentHits 2076→0, unmappedDrumKeys 7→0; ride.wav finally wired
- 2026-08-04 · iter 0 · bootstrap — loop scaffolding, audit-site metrics engine + baseline, backlog seeded (31 items), quarantine, .env untracked

## Blocked on Dylan (the only list you need to read)

- **Rebrand veto window.** Direction boards land in `design/directions/` with a decision in `design/DECISION.md` (also logged here). The loop proceeds with the reviewer+persona winner unless you object — a one-line reply redirects it.

- **Design checkpoint 0 is ready to run.** Type `/design-checkpoint` in a session with me present (~5 min): it builds the component previews and syncs them to a "Drum Town UI" project on claude.ai/design — the first DesignSync call needs your permission grant, which unattended ticks can't do.

- **Rotate the PostHog project token.** `.env` is now untracked and the token no longer ships hardcoded in base.njk, but the old value remains in git history. Rotate it in PostHog settings, then update your local `.env`. (SEC, bootstrap)
- **Getting Started photos** (BL-030 / ISSUES.md #1): stick-grip, setup-posture, the-drum-kit lost their figures because the photos never existed. When you have photos, the loop restores the figure blocks + styles.

## Try this now (iter 30)

Open drum.town on your phone and tap the ☰ — that menu never existed before. Then hit the sun/moon in the header: full dark theme, remembered between visits, and it follows your OS if you never touch it. Both were built and judged this tick.

## Needs eyes (now mostly self-served: since iter 23 the loop screenshots and reads its own changes — entries below remain only where human taste matters)

- **BL-015 rem type** (iter 15): at default browser settings every size is mathematically identical to before. Worth one check: bump your browser's default font size (Settings → Appearance) — the whole site should now scale with it.
- **BL-014 notation widths** (iter 12): HIGH VALUE GLANCE — single-bar staves now size to their note count, so sparse exercises (e.g. first-beat Build-Up 1, the quarter-note hat bars) render narrower and fit phones. Check 2-3 lessons on desktop (staves shouldn't look oddly short) and one on a phone/narrow window (short exercises shouldn't pan; playing a long stave should auto-scroll the playhead into view).
- **BL-013 metronome collapse** (iter 8): on desktop, a small − appears in the widget corner — clicking collapses it to a round ♩ button; on a phone (or narrow window <720px) it starts collapsed. Expand, play, collapse while playing — audio should keep running.
- **BL-003 homepage hero + genre cards** (iter 7): after visiting any lesson, the homepage "Up Next" hero should render single-column (the empty preview pane is gone); genre pages and Where-Next cards no longer show the word "ready" under every title; hero stat reads "217 lessons".

- **BL-010 exercise titles div→h3** (iter 2): open any lesson — exercise titles should look identical (the class controls type/margin; only line-height tightened 1.65→1.2 on multi-line titles). Tab from the address bar: a rust "Skip to content" chip should appear top-left and jump to content.

## Needs ear (audio changes awaiting a listen)

- **BL-039 honest acoustic voices** (iter 28): in the acoustic kit, play `latin-cha-cha` ex 1 (bell should now sound like a bell, clearly synthetic, NOT a sped-up ride), `cymbal-voicings` (ride vs crash vs china all distinct), and `jazz-ride-pattern` in the electronic kit (ride now has a low ping body under the shimmer).

- **BL-002 multi-bar looping** (iter 4): play `flam` ex 1 or `latin-clave-intro` (2-bar patterns) — the pattern should loop cleanly end-to-end with no doubling/echo, and the playhead should cross the whole stave exactly once per loop. Also `jazz-ballad` (dotted rhythms) should sit correctly against the count.
- **BL-001 new drum voices** (iter 1): play `the-drum-kit` "Tour of the Kit" (all voices in one exercise), a `latin-cha-cha` bell pattern, a `metal-headbang` china groove, and `basic-fills` toms. Judge: ride vs hat distinction, bell character, crash/china wash, tom pitch spread. Acoustic-kit crash/china/bell are approximations from existing samples (real WAVs would help — ISSUES.md #2).

## Parked

_(none)_

## Metrics snapshots

**Discovery iter 20 (2026-08-04), after 18 improvement ticks.** Newly zeroed since iter 10: relativeSitemapLocs 227→0 · lessonsZeroExercises 2→0 · pxFontSizes 79→0. Falling: lessonsThinProse 53→43 · lessonsNoSubheadings 68→58 · lessonsMissingListening 77→64 (+31 picks, now 448 entries). **Still open** (all mapped to ready items): focusDistinctValues 144 (BL-024) · thin/subheads/listening long tails (BL-022/023 chunks) · tempoRangeMetaMismatches 6 (BL-027) · titleNumberingSchemes 5 (BL-026) · duplicatePicks 7 (BL-029) · fewExercises 14 (some intentional — setup/mindset lessons; revisit the floor when BL-022 completes) · dark-scheme + reduced-motion (BL-017 + future theme item) · deadFeatureRefs 1 (noted generalistPath, by design). Link scan clean twice running; zero parked items in 19 ticks.

**Discovery iter 10 (2026-08-04), after 9 improvement ticks.** Zeroed since bootstrap: player.silentHits 2076→0 · player.unmappedDrumKeys 7→0 · player.multiBarSpecsUnsupported 153→0 · dom.pagesMissingMain 228→0 · dom.pagesMissingSkipLink 228→0 · dom.svgsMissingAria 888→0 · css.outlineNone 4→0 · css.missingFocusVisible 1→0 · content.britishSpellings 119→0. Improved: hygiene.deadFeatureRefs 4→1 · css.pxFontSizes 84→79. **Still open**: lessonsMissingListening 77 · lessonsNoSubheadings 68 · lessonsThinProse 53 · focusDistinctValues 144 · pxFontSizes 79 · relativeSitemapLocs 227 · duplicateListeningPicks 7 · lessonsFewExercises 14 · zeroExercises 2 · tempoRangeMetaMismatches 6 · titleNumberingSchemes 5 · dark-scheme/reduced-motion groundwork. Link scan: 11,749 internal refs, all resolve.

_(pasted by each discovery pass)_
